# يُنسخ إلى /etc/nginx/conf.d/ (لا sites-available) — limit_req_zone لازم
# سياق http{}، ونطاق sites-available/enabled أضيق من ذلك.
# يستخدمه location /login في nginx-hotel-admin.conf.
limit_req_zone $binary_remote_addr zone=login_limit:10m rate=10r/m;
