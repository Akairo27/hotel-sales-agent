// The operator's own identity is not modelled in the database — one
// deployment serves exactly one hotel company — so the two strings the
// shell prints live here instead of being spelled out at each call site.
// Swap BRAND_NAME for the client's trade name once it is confirmed;
// nothing else in the app reads it.
export const BRAND_NAME = "لوحة التحكم";

export const BRAND_TAGLINE = "المبيعات والتسعير";
