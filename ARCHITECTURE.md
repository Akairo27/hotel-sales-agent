# المعمارية — منظومة تشغيل شركة الفنادق

---

## ١. المبدأ الحاكم

> **نموذج اللغة طبقة محادثة فقط. لا يحسب سعراً، ولا يحجز غرفة، ولا يرى تكلفة.**

كل قرار يتعلق بالمال أو المخزون يُتخذ في خدمات حتمية مكتوبة بكود عادي، قابلة للاختبار، ونتيجتها ثابتة لنفس المدخلات.

النموذج يستدعي هذه الخدمات كأدوات، ويستقبل منها خيارات محدودة، ويتكلم بها فقط. وأي رقم ينطق به يُتحقق منه في السيرفر قبل التنفيذ.

**السبب:** حقن التعليمات هو الخطر الأول في وكيل يبيع للعامة. الرد الوحيد الفعّال هو ألا يملك النموذج الصلاحية أصلاً — لا أن نطلب منه ألا يستخدمها.

### نطاق النظام (قرار 2026-09-24)

النظام **منظومة تشغيل كاملة لشركة فنادق**: فنادق، وكتل غرف متعاقَد عليها، وأسعار، ومخزون، وحجوزات، ولوحة إدارة للموظفين. وكيل واتساب **قناة واحدة** فوق هذه النواة وليس المنتج نفسه: يقرأ منها عبر أدوات محدودة (§٧)، ولا يكتب فيها حصصاً ولا أسعاراً ولا مخزوناً.

**أثر ذلك على التخطيط:** أي ميزة جديدة تُصمَّم أولاً كقدرة في النواة — مسار كتابة في قاعدة البيانات، وشاشة في اللوحة، ودالة قراءة في خدمة — ثم يُمنح الوكيل أداة قراءة فوقها إن احتاجها. ما يصلح للوحة والوكيل معاً (التوفر، والتسعير الحتمي) يُبنى مرة واحدة ويقرؤه الطرفان. المبدأ الحاكم أعلاه لا يتغير، ولا تتغير أي قاعدة في `CLAUDE.md` بسبب هذا التوسيع.

---

## ٢. المكوّنات

```
┌─────────────────────────────────────────────────┐
│  WhatsApp Cloud API  (رقم تجاري واحد)            │
└───────────────┬─────────────────────────────────┘
                │ webhook (موقّع)
┌───────────────▼─────────────────────────────────┐
│  agent-service        (FastAPI)                  │
│  • تحقق التوقيع + idempotency                    │
│  • حالة المحادثة                                 │
│  • استدعاء Gemini + tools                        │
│  • حارس المخرجات (output guard)                  │
└──┬──────────┬──────────┬───────────────┬─────────┘
   │          │          │               │
┌──▼────┐ ┌───▼─────┐ ┌──▼────────┐ ┌───▼────────┐
│pricing│ │inventory│ │ booking   │ │ escalation │
│service│ │ service │ │ service   │ │  service   │
└──┬────┘ └───┬─────┘ └──┬────────┘ └───┬────────┘
   └──────────┴──────────┴──────────────┘
                    │
        ┌───────────▼────────────┐
        │  Supabase / Postgres   │
        │  RLS على كل جدول        │
        └───────────┬────────────┘
                    │
   ┌────────────────┴─────────────────┐
   │                                   │
┌──▼──────────────┐          ┌────────▼─────────┐
│ admin-dashboard │          │  worker (cron)   │
│    (Next.js)    │          │ • إفراج المهل     │
└─────────────────┘          │ • التنبيهات       │
                             │ • تمرير المحاسبة  │
                             └──────────────────┘
```

---

## ٣. التقنيات

| الطبقة | الاختيار | السبب |
|---|---|---|
| قاعدة البيانات | Supabase / Postgres | RLS أصلية، معاملات قوية، خبرة سابقة |
| الخدمات | FastAPI (Python) | خبرة سابقة، اختبارات سهلة |
| الواجهة الإدارية | Next.js | خبرة سابقة |
| النموذج | Gemini | قرار معتمد — خلف واجهة قابلة للاستبدال |
| قناة المحادثة | WhatsApp Cloud API الرسمي | رقم تجاري واحد معتمد |
| المهام المجدولة | worker منفصل + pg_cron | إفراج المهل والتنبيهات |
| المستودع | GitHub | نسخ، مراجعة، تتبع |

### الاستضافة

| المكوّن | الاستضافة |
|---|---|
| قاعدة البيانات | Supabase (أقرب منطقة متاحة) |
| خدمات FastAPI + worker | Railway (أو Fly.io / Render) |
| الواجهة الإدارية | Vercel أو Railway |

**قرار مغلق:** الاستضافة خارج المملكة معتمدة — العميل يملك التصاريح
اللازمة لذلك. لا يُعاد فتح هذا القرار إلا إذا تغيّر وضع التصاريح لدى
العميل.

### ما لا نستخدمه ولماذا
- **n8n**: للتنبيهات والتكاملات الجانبية فقط. **ممنوع** أن يحتوي أي منطق تسعير أو حجز — لأنه غير قابل للاختبار الآلي ولا للمراجعة في Git بشكل موثوق.
- **Redis**: غير ضروري بهذا الحجم. Postgres يكفي للأقفال والطوابير، وكل مكوّن إضافي نقطة فشل جديدة.

---

## ٤. نموذج البيانات

### الجداول الأساسية

**hotels** — الفنادق
`hotel_name` · `distance_to_haram_meters` · `star_rating` · `address_text` ·
`check_in_time` · `check_out_time` · `is_active`

**room_types** — أنواع الغرف لكل فندق
`hotel_id` · `room_type_name` · `capacity_adults` · `bed_configuration` · `size_sqm`

**hotel_amenities** — مرافق الفندق
`hotel_id` · `amenity` — صف لكل مرفق، والمفتاح الأساسي هو الزوج نفسه. لا يوجد
`UPDATE`: المرفق يُضاف أو يُحذف.

**قاعدة المحتوى:** كل ما يُخزَّن عن الفندق منظَّم — رقم، وقت، أو قيمة من قائمة
مغلقة يفرضها `CHECK`. لا يوجد عمود وصف حر عمداً: أي نثر يُخزَّن هنا يدخل سياق
النموذج، وحارس المخرجات (§٧) يتحقق من الأرقام المالية فقط — لا يستطيع أن يكشف
جملة تدّعي مرفقاً لا يملكه الفندق. توسيع قائمة المرافق ميقريشن بسطر واحد، وهذي
هي التكلفة المقصودة.

**الحقول التفصيلية تقبل `NULL`.** الميقريشن ٠٠٢٣ طُبِّق على صفوف موجودة،
والميقريشنات للأمام فقط، ولا توجد قيمة افتراضية صادقة لمسافة فندق حقيقي عن
الحرم. لذلك الوجود غير مفروض في SQL — القيود تفرض *صحة* القيمة لا وجودها.
اللوحة تُلزم بالحقول عند الحفظ وتُظهر شارة "ملف غير مكتمل" لما سبقها. **ربط ما
يعرضه الوكيل باكتمال الملف مسؤولية المرحلة ٤**، عند أول قراءة فعلية لهذي
الأعمدة — وبدونه تظل `search_alternatives` (§٧) بلا أساس تختار عليه.

**seasons** — المواسم — **يعرّفها العميل بالكامل من التقويم**
`name` · `calendar_type` (hijri | gregorian) · `start_month` · `start_day` · `end_month` · `end_day` · `priority` · `is_default`

**القواعد:**
1. البداية داخل، النهاية خارج.
2. عند التداخل يفوز الأعلى `priority` — والعميل هو من يحدد الأولوية. النظام لا يفترض أن الهجري أهم من الميلادي أو العكس.
3. **موسم افتراضي واحد إلزامي** (`is_default = true`) يغطي السنة كاملة بأولوية صفر. أي تاريخ لا يقع في موسم معرّف يرجع إليه.

**الأثر:** لا يمكن أن يوجد تاريخ بلا سعر. الفجوات في تعريف العميل تُغطى تلقائياً بالافتراضي بدل أن يفشل التسعير. هذا يزيل الحاجة لحسم الفجوات قبل الإطلاق.

**شاشة المواسم (phase 3 PR C، `admin/app/seasons/`):** محرر مواسم مع
معاينة تقويمية حية — تسمح للعميل يشوف أثر كل تعديل فوراً قبل الحفظ.
المعاينة تعيد تنفيذ منطق `season_contains`/`resolve_season_id`
(`services/pricing/seasons.py`) بلغة TypeScript
(`admin/lib/seasonCalendar.ts`) بدل استدعاء الخدمة حياً — لا يوجد اتصال
بين `admin/` وأي خدمة Python. القاعدة ٦ (تحويل هجري بوحدة واحدة فقط) لا
تُنتهك: كل حقيقة هجرية (طول الشهر، بداية الشهر ميلادياً) تأتي من جدول
مرجعي ثابت مولَّد مسبقاً (`admin/lib/hijriCalendarReference.json`، عبر
`tests/generate_hijri_calendar_reference.py`)، لا من حساب مباشر.

**ضمان التطابق بين الطرفين، مفروض في CI:** `tests/season_conformance_fixtures.json`
مولَّد من `season_contains`/`resolve_season_id` الحقيقيين
(`tests/generate_season_conformance_fixtures.py`)، ومُختبَر ضد
`seasonCalendar.ts` في `admin/lib/seasonCalendar.conformance.test.ts`
(Vitest). أي تعديل في أحد الطرفين بدون تحديث الآخر يكسر أحد الفحصين —
مُلزَم بهذا عشان معاينة المواسم ما تصير تكذب على العميل وهو يبني تعريفات
مواسمه عليها.

**allotments** — الحصة المخصصة
`hotel_id` · `room_type_id` · `date` · `total_rooms` · `cost_per_night`

**room_night_inventory** — الوحدة الذرية
`allotment_id` · `date` · `total` · `reserved` · `held`
```sql
CONSTRAINT inventory_never_oversold
  CHECK (reserved + held <= total AND reserved >= 0 AND held >= 0)
```
هذا القيد هو خط الدفاع الأخير ضد البيع المزدوج. لا يُزال ولا يُعطّل.

**price_rules** — قواعد التسعير
`scope` (global | season | hotel | room_type) · `scope_id` · `target_margin` · `min_profit_by_lead_time` (jsonb) · `demand_curve`

**quotes** — سجل غير قابل للتعديل
كل سعر نُطق به: التاريخ، رقم العميل، الليالي، سعر العرض، الحد الأدنى المسموح، القاعدة المنتِجة، معرّف المحادثة.
`INSERT` فقط — لا `UPDATE` ولا `DELETE`.

**holds** — الحجوزات المؤقتة
`expires_at` · `released_at` — يفرجها الـ worker تلقائياً

**bookings** — الحجوزات المؤكدة
`status` · `payment_status` · `accounting_sync_status`

**conversations** / **messages** — سجل المحادثات
**escalations** — حالات التصعيد وزمن الاستجابة
**audit_log** — كل تغيير في القيم المالية: من، متى، القيمة قبل وبعد

### عقود الفنادق وأوراق الأسعار — تصميم معتمد (2026-09-24)، غير منفَّذ

هذا القسم **قرارات تصميم لا تنفيذ**: لا جدول ولا شاشة مما يرد هنا موجود
اليوم في `db/migrations/` أو `admin/`. سُجّل قبل أول ميقريشن لأن الشكل
الخاطئ لهذه الجداول مؤلم التصحيح بعد أن تتراكم عليه بيانات (نفس منطق §١٠).

**المشكلة.** لا يوجد في المستودع أي مسار ينشئ صفاً في `allotments` أو
`room_night_inventory`؛ المسار الوحيد الموجود يعدّل تكلفة صف قائم
(`admin_set_allotment_cost`). فلا يمكن بيع أي ليلة. الأسعار تصل من الفنادق
أوراقاً (PDF أو Excel) بتخطيط مختلف لكل فندق، وأعداد الغرف تُدخَل يدوياً (§٩).

**القرارات:**

1. **الورقة عقد حجز مسبق.** كل فندق/فترة مستورد يُعامَل «كتلة تعاقدية»
   (`contract block`): غرف محجوزة لشركتنا، لا قائمة أسعار عند الطلب.
2. **السعر المستورد هو سعر البيع الأساسي (`price_type = sell`) — إجابة
   العميل بتاريخ 2026-09-24.** تشتري الشركة الغرف بسعر (التكلفة) وتبيعها
   بسعر آخر يحكمه العرض والطلب؛ وسعر الورقة هو **سعر البيع الأساسي**
   المحدَّد في بداية الموسم، وقد يساويه السعر المعروض على العميل أو يُعدَّل
   بحسب الموسم والطلب. الحقل `price_type` يبقى صريحاً في كل دفعة ولا
   يُفترض. سعر البيع لا يُكتب في `allotments.cost_per_night` (وجهة كتابته في
   «قرارات التسعير والتخزين» أدناه). **التكلفة موجودة منفصلةً**، ومصدرها (ملف آخر أم
   إدخال يدوي) ما زال مفتوحاً مع العميل؛ ويبقى `cost_per_night` قابلاً
   للقيمة الفارغة في الأثناء. المبدأ الحاكم (§١) وقاعدة «التكلفة لا تدخل سياق
   النموذج» تسريان كما هما على أي تكلفة: النموذج لا يرى تكلفة أيّاً كان
   مصدرها، وسعر البيع يصله عبر `get_quote` بعد أن يحسبه الكود.
3. **لا نموذج لغة في الاستخراج — قرار الإصدار الأول، للدقة.** الاستخراج
   حتمي بالكامل: قارئ Excel (`.xlsx` فقط، بلا `.xls`) وقارئ PDF لطبقته
   النصية، بمفردات عناوين أعمدة عامة مغلقة (`WD`، `WE`، `BED`، `DBL`، `TPL`،
   `QUAD`، `RATE`، `BB`، `RO` ...) لا محلل لكل قالب؛ وما لا يُعرف من الأعمدة
   يعيّنه الموظف في شاشة المراجعة. الصفحات بلا طبقة نصية تُسجَّل «غير
   مقروءة» ولا تُتخطى بصمت؛ لا OCR ولا رؤية حاسوبية. الحد الأقصى للملف
   ١٠ ميجابايت. أوراق البيع لا تحمل تكلفة، فسبب المنع فيها دقة الأرقام؛ أما
   ملفات التكلفة (إن كان مصدر التكلفة ملفاً، القرار ٢) فتسري عليها قاعدة
   «التكلفة لا تدخل سياق النموذج» (القاعدة ٢ في `CLAUDE.md`). فيبقى
   الاستخراج حتمياً في الحالتين.
4. **فترات الورقة ليست `seasons`.** المواسم يعرّفها العميل من التقويم
   وتقود هوامش الربح (§٤ أعلاه)؛ أما فترات الورقة فتتحول إلى صفوف أسعار
   لكل ليلة. الاستيراد لا يكتب في `seasons` إطلاقاً.
5. **الليلة هي الوحدة، والغياب يعني «غير قابلة للبيع».** التسعير والتوفر
   يقرآن صفاً لكل (فندق، نوع غرفة، ليلة). ما لا يستطيع المخطط التعبير عنه
   (باقة فترة كاملة، صفحة غير مقروءة، إغلاق) لا يُكتب له صف، فتبقى الليلة
   مغلقة بدل أن تُسعَّر خطأً.
6. **كل إشغال أو نوع غرفة في الورقة نوع غرفة مستقل بمخزونه.** (`DBL`،
   `TPL`، `QUAD`، `SUP`، `SUITE`، `J.SUITE`) تصير أنواع غرف، و`capacity_adults`
   من الإشغال حيث ينطبق. **`SUITE` و`J.SUITE` نوعا غرفة مختلفان** (قرار
   العميل). **`SUP` = `SUITE`** (جواب العميل بتاريخ 2026-09-25): يُطابَق
   `SUP` مع نوع الغرفة `SUITE` عند الاستخراج؛ يقول العميل إن بينهما فرقاً
   طفيفاً لكنه يريد معاملتهما نوعاً واحداً. **حالة حدّية:** إن احتوت ورقة
   واحدة على `SUP` و`SUITE` بسعرين مختلفين للفترة نفسها فلا يُدمجان بصمت؛
   تُمنع الدفعة بتحذير **مانع** ويقرر المراجع.
   **`FAREAST` ليس نوع غرفة:** هو باقة إقامة بنظام الإقامة الكاملة
   (`full board`) **مقيَّدة بجنسيات شرق آسيا وجنوبها الشرقي** (ماليزيا
   وإندونيسيا وتايلاند والفلبين وغيرها؛ القائمة الدقيقة لا جواب مسجَّلاً لها
   بعد)، فيُنمذَج **خيار وجبات مرتبطاً بسعر** لا نوع غرفة. **يجوز للوكيل
   عرضه** (قرار العميل). **وجباتها تُسعَّر للفرد في الليلة، ويختلف سعر
   الوجبة بحسب مستوى الفندق** (جواب العميل بتاريخ 2026-09-25). **الخطة
   (اتجاه معتمد من المالك؛ لا بناء بعد):** (أ) **سعر وجبة للفرد في الليلة
   يُخزَّن لكل فندق ويدخله الموظف يدوياً** (بالهللات، عدد صحيح)؛ (ب) **سعر
   الباقة = سعر الغرفة + (سعر الوجبة للفرد × عدد النزلاء × عدد الليالي)**،
   والمقترح ألا تُطبَّق تعديلات الموسم والطلب على مكوّن الوجبات؛ (ج)
   `bot_sellable` يبقى `false` **حتى يُدخَل سعر وجبة الفندق**؛ (د) فحص جنسية
   حتمي في الكود لا يقرره النموذج: قائمة الجنسيات المؤهلة تُخزَّن مع الباقة،
   والجنسية التي يصرّح بها العميل تُحفظ في حالة المحادثة في قاعدة البيانات
   (لا في سياق النموذج) وتُسجَّل في الحجز (`bookings.nationality` موجود
   أصلاً)، ويبقى التحقق من الوثائق عند الوصول لموظف. وأي أداة أو معامل جديد
   يستدعيه النموذج يحتاج موافقة (§١٠ في `CLAUDE.md`). **غير محسوم:** ما
   يمثّله الرقم في خانة `FAREAST` داخل الورقة نفسها (سعر غرفة أم وجبة أم
   مجموع)؛ وإلى أن يُحسم تُستخرج الخانة للمراجعة فقط ولا يُكتب منها أي سعر.
7. **العطلة الأسبوعية إعداد لكل فندق**، افتراضيه الجمعة والسبت، لا ثابت في
   الكود. أعمدة مقترحة على `hotels`: `weekend_days`، و`city` بقائمة
   مغلقة يفرضها `CHECK` (نفس قاعدة المحتوى المنظَّم أعلاه). وهذان الحقلان
   وسائر حقول ملف الفندق (المدينة والمنطقة والحي والنجوم والمسافة والمرافق)
   **يدخلها الموظف يدوياً، ولا يكتب الاستيراد أياً منها** — يُعرض ما تذكره
   الورقة (مثل «400 From Haram») تلميحاً للقراءة فقط في شاشة المراجعة. وفندق
   ينشئه الاستيراد يبقى ملفه ناقصاً فلا يعرضه الوكيل قبل أن يكمله موظف. انظر
   «وصف موقع الفندق» أدناه.
8. **نهاية الفترة شاملة أم غير شاملة؟** الأوراق الفعلية غير متسقة (فترتان
   تتلاقيان في اليوم نفسه بجانب صفوف تبدو شاملة النهاية)، فلا يُفترض
   أحدهما. الاختيار قرار المراجع لكل دفعة، يسبقه كشف تلقائي، ويوقف
   الاعتماد تحذير **مانع** عند الغموض. النظام نفسه يعتمد نهاية مستثناة
   (قواعد المواسم أعلاه)، فالتحويل صريح ومُختبَر عند الحدود.
9. **السنة المفقودة** (مثل «14 Oct–12 Nov») تُستنتج من موسم الورقة أو تاريخ
   تحديثها، وتُعلَّم دائماً ولا تُعتمد إلا بتأكيد المراجع؛ لا استنتاج صامت.
10. **الصفوف الحمراء = إغلاق بيع** (قرار العميل لورقة DoubleTree). تُستورد
    فترات مغلقة لا أسعاراً. هذا التفسير يُقترح ويؤكده المراجع لكل دفعة، ولا
    يُعمَّم على ورقة أخرى بصمت. تطبيق الإغلاق داخل `check_availability`
    و`create_hold` تعديل في `inventory/` يحتاج موافقة منفصلة؛ وإلى أن يتم،
    يمنع الاعتماد أي إغلاق تتقاطع معه ليلة فيها غرف قابلة للبيع.
11. **أعداد الغرف يدوية دائماً.** لا يُنشئ الاستيراد أي عدد غرف. يدخلها
    موظف (فندق + نوع غرفة + مدى تواريخ ← عدد الغرف) في شاشة مخصصة: تُرفَض
    أي ليلة بلا سعر («لا سعر ← لا غرف») وأي خفض دون `reserved + held`،
    والعملية كلها أو لا شيء، وتُدقَّق. الاستيراد لا يمس `reserved` ولا `held`
    ولا `total`.
12. **الإضافات والباقات** (سرير إضافي، وجبات للفرد، سحور وإفطار، سعر ثابت
    لفترة كاملة) **تُخزَّن كشروط عقد ولا تُسعَّر في الإصدار الأول.** الوكيل
    لا يعد بها ويحيل طلبها إلى موظف. تسعيرها يغيّر نموذج الحساب «لكل ليلة»
    في `pricing/` فيحتاج موافقة منفصلة، ويُخطَّط كمرحلة لاحقة **تنتهي قبل
    رمضان القادم (تقريباً بداية فبراير ٢٠٢٧)**. ويشمل ذلك خيار الوجبات
    `FAREAST` (القرار ٦): يُخزَّن شرطَ عقد بلا صف لكل ليلة، فلا يُسعَّر ولا
    يعرضه الوكيل بحكم البناء لا بحكم تعليمات، ويبقى `bot_sellable = false`
    حتى يُدخَل سعر وجبة الفندق (القرار ٦).

**بنود لا جواب مسجَّلاً لها بعد في هذا المستند (لا يُبنى عليها منطق قبل
تسجيل الجواب):**
- مصدر التكلفة: ملف آخر أم إدخال يدوي؟ (القرار ٢)
- كيف يُعبَّر عن التعديل الموسمي على سعر البيع (نسبة فوق سعر الورقة لكل
  موسم؟) وهل يجوز أن يهبط السعر المعروض دون سعر الورقة؟ (القرارات ٢ و٣ في
  «قرارات التسعير والتخزين»)
- `FAREAST`: القائمة الدقيقة للجنسيات المؤهلة، وماذا تشمل الإقامة الكاملة،
  وما يمثّله الرقم في خانة `FAREAST` داخل الورقة (القرار ٦).

أُغلق بجواب العميل (2026-09-24): `price_type = sell` (سعر البيع الأساسي)،
و`FAREAST` باقة وجبات لا نوع غرفة، مقيَّدة بجنسيات شرق آسيا وجنوبها
الشرقي، ويجوز للوكيل عرضها، و`SUITE` و`J.SUITE` نوعا غرفة مختلفان،
والفئة = عدد النجوم، وموقع الفندق يوصف بمناطق.

أُغلق بجواب العميل (2026-09-25): `SUP` = `SUITE` (يُطابَق عند الاستخراج،
ويُمنع ما فيه سعران مختلفان للفترة نفسها ويقرر المراجع)، ووجبات `FAREAST`
تُسعَّر للفرد في الليلة ويختلف سعرها بحسب مستوى الفندق.

**دورة الدفعة.** رفع ← استخراج (مهمة في الـ worker، §٦) ← تحقق حتمي ← مراجعة
وتعديل بشري ← اعتماد ← تراجع عند الحاجة. الجداول المخططة: الدفعات
(`rate_import_batches`)، وملفات المصدر (`rate_import_files`، تُخزَّن في قاعدة
البيانات)، والصفوف المرحلية (`rate_import_rows`)، والليالي الموسَّعة للمعاينة
(`rate_import_nights`)، والأحداث (`rate_import_events`، إلحاق فقط)، وسجل
التطبيق (`rate_import_applied`، صورة قبل/بعد لكل صف). كلها RLS رفض افتراضي،
ولا يصلها إلا المدير؛ ودفعات `price_type = cost` (إن جاءت التكلفة بملف)
تشترط أيضاً `can_view_cost` لأنها تحمل تكلفة.
- **لا يُكتب شيء في جداول التسعير أو المخزون الحية قبل اعتماد صريح.**
- **الاعتماد معاملة واحدة:** يقفل الدفعة، ويشترط حالة «تم التحقق» وتطابق
  بصمة الصفوف مع ما جرى التحقق منه (فتعديل بعد التحقق لا يُعتمد)، واعتماد
  ثانٍ للدفعة نفسها يفشل فيتم التنفيذ مرة واحدة. يقفل صفوف
  `room_night_inventory` بترتيب `stay_date` (ترتيب `create_hold` نفسه)،
  ويكتب الأسعار وحدها ولا يمس أي عدد غرف؛ ووجهة الكتابة في «قرارات التسعير والتخزين» أدناه.
- **التراجع** يستعيد صورة «قبل» لكل صف ما لم يتغير منذ الاعتماد؛ وعند أي
  تعارض يسرده ويرفض. الصفوف التي أنشأتها الدفعة تُعطَّل ولا تُحذف (لا
  `DELETE` ممنوح، كما في `hotels` و`room_types`)، وبشرط ألا تكون عليها غرف
  محجوزة أو مثبّتة.

**قرارات التسعير والتخزين** (١–٣ معتمدة من المالك كخطة فقط بتاريخ
2026-09-24، و٤ ما زال مفتوحاً. لا بناء قبل موافقة صريحة، وما يمس `pricing/`
أو `inventory/` أو المخطط أو RLS يحتاج موافقة منفصلة):

1. **أين يُخزَّن سعر البيع؟** `allotments.cost_per_night` اليوم `NOT NULL`،
   فلا يمكن إنشاء صف ليلة بسعر بيع وحده دون اختلاق تكلفة. **الاتجاه
   المقترح، معتمد كخطة فقط (لا تغيير في `pricing/` ولا ميقريشن قبل موافقة
   صريحة):** عمود جديد `allotments.sell_price_per_night` مع جعل
   `cost_per_night` يقبل `NULL`، والتكلفة موجودة منفصلة ومصدرها مفتوح مع
   العميل. سجل `quotes.nights` يقبل أصلاً ليلة بلا تفاصيل تكلفة
   (`override_applied`، ميقريشن ٠٠٠٩)، لكنه يحتاج علامة أوضح من إعادة
   استخدام «تجاوز يدوي». **البدائل:** (أ) `price_overrides`: لا تعديل في
   `pricing/`، لكنه يخلط الاستثناء اليدوي بأسعار العقد ويشترط `expires_at`
   ويبقى `cost_per_night` إلزامياً؛ (ب) اشتقاق التكلفة من سعر البيع وعمولة
   معروفة — تبقى موثَّقة إن أكّد العميل نموذج عمولة، ووصف العميل الحالي
   (تكلفة مستقلة وسعر بيع يحكمه العرض والطلب) لا يذكرها.
2. **التعديل الموسمي وتعديل الطلب يُطبَّقان فوق سعر الورقة** (قرار العميل؛
   يحل محل اقتراح سابق بلا هامش ولا معامل طلب). **معتمد كخطة فقط
   (المالك):**
   `ask = سعر_الأساس × (١٠٠٠٠ + sell_adjustment_bps) ÷ ١٠٠٠٠ × معامل_الطلب`
   بحساب صحيح بالهللات وبقاعدة التقريب الحالية نفسها؛ ومعامل الطلب هو
   الموجود اليوم (معاملا الإشغال والمدة المتبقية) لأنه نسبي أصلاً؛ و
   `sell_adjustment_bps` عمود جديد قابل للقيمة الفارغة على `price_rules`
   (فارغ = بلا تعديل) يُحلّ بسلسلة التوريث نفسها (عام ← موسم ← فندق ← نوع
   غرفة). هذا يمس `pricing/` والمخطط فيحتاج موافقة منفصلة. الليالي التي لها
   تكلفة بلا سعر أساس تبقى على المعادلة الحالية «تكلفة + هامش».
3. **الحد الأدنى المسموح (`min_allowed`).** **معتمد كخطة فقط (المالك):**
   (أ) *حين يوجد سعر أساس وتكلفة معاً:* `min_allowed = التكلفة + الحد
   الأدنى للربح` (حسب المدة المتبقية) كما هو اليوم، فلا ينزل السعر عن
   التكلفة أبداً، ومعامل الطلب لا يمسه؛ وإن نتج `ask` أدنى منه فهو خطأ
   إعداد صريح (`InconsistentPriceConfigurationError`) كما هو اليوم بلا رفع
   صامت، ويمنع التحقق عند الاستيراد سعراً أساسياً أدنى من التكلفة.
   (ب) *حين يوجد سعر أساس بلا تكلفة:* لا حد أدنى مبني على التكلفة، فلا يهبط
   `ask` دون سعر الأساس (يُقيَّد معامل الطلب الأدنى من ١ عند ١)، و
   `min_allowed = ask` أي بلا تفاوض (`negotiation_open = false`)، وهذا يطابق
   الوكيل اليوم (بلا مسار تفاوض) ويفشل مغلقاً؛ وحين تصل التكلفة لتلك الليلة
   تنتقل تلقائياً إلى (أ). **بديل لا يُتابَع حالياً (قرار المالك):** حد أدنى
   بنسبة من سعر الأساس تحددها قاعدة، لتفاوض محدود بلا تكلفة. شرط الاختبار «الحد
   الأدنى لا ينزل عن التكلفة» في `CLAUDE.md` يسري على (أ)، ويُستبدل في (ب)
   باختبار `min_allowed == ask` وإغلاق التفاوض.
4. `INSERT ... ON CONFLICT DO UPDATE` بدور `authenticated` قد يتطلب صلاحية
   `SELECT` أوسع مما يسمح به إخفاء التكلفة (القاعدة ١١ في `CLAUDE.md`).
   يتوقف هذا على القرار ١؛ ويُتحقق منه على Postgres حقيقي قبل تثبيت الشكل.
   البديل دالة `SECURITY DEFINER` ببوابة داخلية، وهي تغيير في تصميم RLS
   يحتاج موافقة منفصلة.

### وصف موقع الفندق للبحث والتصفية (2026-09-24)

**قرارات العميل والمالك:**
- **الفئة = عدد النجوم** (`hotels.star_rating` الموجود). «اقتصادي» يعني ٣
  نجوم فأقل؛ هذا التعيين ثابت واحد في الكود لا في المخطط. لا حقل مستوى جديد
  ولا `tier_rank`.
- **الموقع يوصف بمناطق لا بأمتار.** مكة: المنطقة المركزية (حول الحرم أو على
  ساحاته أو قربه) مقابل خارجها. المدينة المنورة: المنطقة المركزية، ثم
  الشمال والغرب والجنوب، ثم خارج المنطقة المركزية. الفنادق الاقتصادية
  تُوصف بالحي أو الشارع، وفنادق الخمس نجوم تُعرف عادة بمسافتها من الحرم.
- **كل هذه الحقول يدوية**: لا يكتب الاستيراد أياً منها (القرار ٧ أعلاه).
- **قائمة الاكتمال** (`REQUIRED_HOTEL_PROFILE_FIELDS`) لا تتغير حتى يصدر
  البحث بين الفنادق؛ ولا تغيير في الوكيل الآن.
- المسافة `distance_to_haram_meters` تبقى اختيارية باسمها الحالي، ويوثَّق
  بـ`COMMENT ON COLUMN` أن مرجعها يتبع مدينة الفندق (المسجد الحرام لمكة،
  والمسجد النبوي للمدينة). لا عمود لدقائق المشي.

**معتمد كما هو مقترح (المالك، 2026-09-24)؛ يدخل في PR إدخال الأسعار والغرف
اليدوي بعد PR الأدوار، ولا بناء بعد:** ميقريشن واحد يضيف إلى `hotels`
أعمدة قابلة للقيمة الفارغة أو ذات قيمة افتراضية فقط، بلا إعادة تسمية:
- `city`: `makkah` أو `madinah` (قائمة مغلقة بـ`CHECK`).
- `weekend_days`: `smallint[]` افتراضيه الجمعة والسبت (`{5,6}`)، بقيد
  تعبيري خالص بلا دالة تحقق مخصصة.
- `zone`: قائمة مغلقة مقرونة بالمدينة، وقيدها يربط المنطقة بمدينتها:
  `makkah_central`، `makkah_outside`، `madinah_central`، `madinah_north`،
  `madinah_west`، `madinah_south`، `madinah_outside`. إضافة منطقة لاحقاً
  ميقريشن بسطر واحد، وهذه هي التكلفة المقصودة (نفس منطق قائمة المرافق).
- `district_name`: اسم الحي أو الشارع، نص قصير اختياري (١ إلى ٦٠ حرفاً) يُعرض
  كما هو ولا يُستعمل تعليمةً. هو اسم علم يدخله الموظف، وله سابقة في
  `address_text`؛ وهو خروج محدود عن قاعدة «لا نص حر» أعلاه وافق عليه المالك
  (اسم علم يدخله المدير وطوله محدود).

**تغذية البحث لاحقاً (لا تغيير الآن):** التصفية بالمدينة والمنطقة والنجوم
(اقتصادي = ٣ فأقل) والمسافة الاختيارية؛ والترتيب حسب المسافة يصح داخل
المدينة الواحدة فقط لأن المرجع يختلف بين المدينتين؛ وتُستبعد الفنادق ذات
الملف غير المكتمل. المعاملات الجديدة للأداة (المدينة، المنطقة، الحد الأقصى
للنجوم...) تحتاج موافقة عند بنائها.

---

## ٥. خدمة التسعير

### الواجهة
```
POST /pricing/quote
IN : hotel_id, room_type_id, check_in, check_out, rooms
OUT: {
  ask_price_total,        # سعر العرض
  min_allowed_total,      # أدنى سعر مسموح به
  nights: [ {date, season_id, ask, min_allowed} ],
  negotiation_open: bool,
  quote_id
}
```

### قواعد صارمة
1. **لا تُرجع `cost` أبداً.** غير موجودة في الاستجابة، ولا في سياق النموذج.
2. التسعير **لكل ليلة** ثم يُجمع. الإقامة التي تعبر موسمين تُسعّر بموسمين.
3. `negotiation_open` تُحسب من المدة المتبقية للوصول فقط. خارج النافذة = `false`، والنموذج لا يملك أي مساحة.
4. الطلب يحرّك `ask_price` فقط. `min_allowed` لا يتأثر بالطلب إطلاقاً.
5. كل استدعاء يُسجَّل في `quotes` قبل الرد.

### تسلسل الحساب
```
cost
  → + target_margin (من قواعد التوريث: عام ← موسم ← فندق ← نوع غرفة)
  → × معامل الطلب (نسبة التصريف × المدة المتبقية)
  → = ask_price

cost
  → + min_profit (حسب المدة المتبقية والموسم)
  → = min_allowed

تجاوز يدوي إن وُجد ← يتقدم على الاثنين، لمدة محددة
```

---

## ٦. خدمة المخزون والحجز

### الحجز المؤقت
```
BEGIN;
  SELECT * FROM room_night_inventory
    WHERE allotment_id = ANY(...) AND date BETWEEN ...
    FOR UPDATE;                          -- قفل الصفوف

  -- التحقق من التوفر لكل ليلة
  UPDATE room_night_inventory SET held = held + :rooms ...;
  INSERT INTO holds (...) RETURNING id;
COMMIT;
```
لو انتهك القيد، تفشل المعاملة كاملة. **لا يوجد تحقق في طبقة التطبيق يُعتمد عليه وحده.**

**`idempotency_key`:** `create_hold` يتطلب مفتاحاً فريداً لكل طلب (عمود
`holds.idempotency_key`، `UNIQUE`) — إعادة نفس الطلب بنفس المفتاح ترجع
نفس الحجز بدل ما تنشئ حجزاً ثانياً. خدمة المخزون لا تولّد هذا المفتاح؛
المستدعي هو المسؤول عنه.

قرار للمرحلة ٤/٥ (طبقة الوكيل)، موثّق هنا عشان ما يُعاد اكتشافه وقت
البناء: المصدر هو `message_id` من واتساب مباشرة — فريد أصلاً حسب §٨،
فلا يحتاج تحويل. **إن صار عندنا أكثر من عملية idempotent على نفس
الرسالة مستقبلاً** (مثلاً `create_hold` و`confirm_booking` بنفس
المحادثة)، يتحول لـ namespace بدل الخام: `f"create_hold:{message_id}"`.

### مدة المهلة
| قبل الوصول | المهلة |
|---|---|
| > ١٥ يوم | ٤٨ ساعة |
| ٣ – ١٥ يوم | ١٢ ساعة |
| < ٤٨ ساعة | ساعتان + دفع كامل |

### التأكيد
`held → reserved` في معاملة واحدة، بعد تأكيد الدفع فقط.
**الكاش عند الوصول لا يؤكده الوكيل** — يفتح تصعيداً بشرياً.

### الإفراج
worker يعمل كل دقيقة: كل `hold` منتهٍ → `held -= rooms`, `released_at = now()`.

**المُشغِّل:** `python -m services.worker` (`services/worker/runner.py`) —
تمريرة واحدة ثم خروج، تُطلقها وحدة systemd (`ops/hotel-worker.service`
و`ops/hotel-worker.timer`) بعد دقيقة من نهاية كل تمريرة، فلا تتداخل
التمريرات. الإفراج آمن عند التشغيل المتزامن: كل حجز يُحجز بـ`UPDATE`
مشروط، فتكرار التشغيل قد يضاعف العمل لكنه لا يضاعف الإفراج. مهام الـ worker
المستقبلية (استخراج أوراق الأسعار، §٤) تُضاف إلى التمريرة نفسها.

**حادثة مسجَّلة (2026-09-24):** الدالة `release_expired_holds` كانت موجودة
لكن لا شيء يستدعيها — لا وحدة ولا مؤقّت ولا cron على الخادم، و`pg_cron`
متاحة غير مثبّتة، ولا دالة SQL تحرّر الحجوزات — فكان أي حجز مؤقّت ينتهي
ستبقى غرفه محجوزة إلى الأبد. لم يترتب أثر فعلي لأن جدول `holds` كان فارغاً.
تثبيت الوحدتين على الخادم خطوة يدوية موثّقة في ترويسة الملفين، ولا تتم
بمجرد الدمج.

---

## ٧. طبقة الوكيل

### الأدوات المتاحة للنموذج
| الأداة | ما تفعله |
|---|---|
| `check_availability` | التوفر لتواريخ محددة |
| `get_quote` | سعر العرض والحد الأدنى المسموح |
| `create_hold` | حجز مؤقت |
| `confirm_booking` | تأكيد بعد الدفع |
| `escalate` | تحويل لبشر |
| `search_alternatives` | فنادق بديلة بنفس التواريخ |

**النموذج لا يملك أي أداة كتابة مباشرة على قاعدة البيانات.**

### استقبال الرسائل: تأكيد سريع + معالجة خلفية

`POST /webhook/whatsapp` لا يعالج الرسالة بالكامل قبل الرد على واتساب.
الجزء السريع فقط (تحقق التوقيع، تخزين الرسالة الواردة، سقف المعدل
اليومي) يعمل بشكل متزامن ثم يرد فوراً بـ`{"status": "accepted"}`؛
استدعاء النموذج، حارس المخرجات، والإرسال الفعلي تعمل بعدها في مهمة
خلفية (FastAPI `BackgroundTasks`، `services/agent/webhook.py`'s
`_generate_and_deliver_reply`). السبب: حادثة فعلية استغرق فيها استدعاء
Gemini الواحد (بعد إعادة المحاولة الداخلية في `services/agent/llm/
client.py`) نحو ٣٢ ثانية، فأعاد واتساب تسليم نفس الرسالة أثناء المعالجة
لأن الرد لم يصل بعد — لا حد زمني كان يضبط طول رد الـwebhook قبل هذا
التغيير.

**الموثوقية عند إعادة تشغيل الخدمة:** إعادة تشغيل رشيقة (`systemctl
restart`، أي SIGTERM) تنتظر انتهاء أي مهمة خلفية قيد التنفيذ قبل إغلاق
العملية، بحدود `TimeoutStopSec` في وحدة systemd (٩٠ ثانية اليوم — هامش
كافٍ فوق أطول استدعاء مُلاحَظ). إيقاف قسري (SIGKILL، تعطل، نفاد ذاكرة)
يفقد أي مهمة قيد التنفيذ — نفس المخاطرة التي كانت موجودة أصلاً لأي طلب
متزامن قيد التنفيذ، لا مخاطرة جديدة، لكنها أيضاً غير محلولة بهذا
التغيير.

**عمل متروك للمستقبل، غير منفَّذ اليوم:** مهمة تصالح دورية (في
`services/worker`) تبحث عن رسالة واردة بلا رد أو تصعيد بعد مهلة معينة
وتفتح تصعيداً لها — لسدّ فجوة الفقد الصامت عند التعطل القسري.

### حارس المخرجات
قبل إرسال أي رسالة للعميل، يمر النص على فحص برمجي:
1. استخراج أي أرقام مالية في النص
2. مطابقتها مع `quote` نشط في هذه المحادثة (من الجلسة الحالية فقط، انظر "الجلسة" أدناه)
3. رقم غير مطابق، أو أقل من `min_allowed`، أو **مذكور بعملة غير الريال، أو
   بلا أي عملة مذكورة إطلاقاً** → **الرسالة تُحجب**، ويُفتح تصعيد، ويُسجَّل
   الحادث

هذه آخر طبقة دفاع. تعمل حتى لو انهار النموذج تماماً.

**التنفيذ:** `services/agent/output_guard/`. مطابق للخوارزمية أعلاه حرفياً،
مع تحديدين ضمن نطاق المرحلة ٤ الحالية (لا مفاوضة إطلاقاً — انظر
`prompt.py`'s `no_booking_actions`):
- "`quote` نشط" تُقرأ كل عروض الأسعار المرتبطة بهذه المحادثة
  (`quotes.conversation_id`)، لا عرضاً واحداً "نشطاً" — عمود
  `conversations.active_quote_id` موجود لكنه لا يُكتب في أي مكان اليوم،
  وربطه يحتاج تعديلاً منفصلاً لا يمسّه هذا التنفيذ.
- بما أن النموذج لا يملك أي مسار تفاوض شرعي اليوم، فالمبلغ الشرعي
  الوحيد هو مطابقة تامة لما أرسلته `quote_to_tool_result` فعلياً — لا
  قيمة مشتقة حسابياً، ولو كانت صحيحة رياضياً.

**عملة السعر:** رقم ملاصق لعلامة عملة أجنبية (USD، $، دولار، ريال قطري، ...)
يُحجب دائماً بصرف النظر عن قيمته — الأسعار في هذا النظام بالريال السعودي فقط
(`prompt.py`'s `prices_are_saudi_riyals_only`)، فلا يوجد سيناريو شرعي لسعر
بعملة أخرى. ورقم مطابق لقيمة حقيقية لكنه بلا أي علامة عملة على الإطلاق —
لا ريال ولا عملة أجنبية — يُحجب أيضاً بدل قبوله كمطابقة نظيفة: تعداد كل
عملات العالم لا يمكن أن يكون كاملاً أبداً، بينما اشتراط العملة الوحيدة التي
يستخدمها هذا النظام فعلاً يغلق الثغرة نفسها بلا تعداد.

**تجاور العلامة بالرقم:** لا نافذة بحث بعدد أحرف ثابت حول الرقم — العلامة
يجب أن تلامس الرقم مباشرة، ولا يفصل بينهما إلا علامات ترقيم أو مسافات؛ وجود
حرف أو رقم آخر في الفاصل يعني أنهما غير متجاورين فعلياً. هذا ما يمنع رقماً
آخر في الجملة نفسها — مثل عدد الليالي في "المجموع ١٬٣٥٠٫٠٠ ريال لـ٣ ليالٍ" —
من أن يُقرأ خطأً كسعر لمجرد وقوعه قريباً من كلمة العملة.

**رقم مجرد بلا علامة عملة (ثغرة موثقة سابقاً، أُغلقت جزئياً):** رقم صحيح
بلا تجميع ألوف وبلا كسر عشري ("سعري ٩٠٠") لا شكل يميّزه عن عدد غرف أو
تاريخ أو رقم هاتف — القياس وحده لا يكفي (اختبار "٣٥٠ متراً من الحرم" في
`tests/unit/test_output_guard_extraction.py` يثبت هذا مباشرة: نفس الشكل
تماماً). الحل: هذا الرقم يُقترح كمرشّح فقط عندما يساوي **تماماً** — لا
أقل من — قيمة حقيقية مسعّرة لهذه المحادثة (مبلغ مسموح أو `min_allowed`
نفسه)، لا أي رقم أقل من الحد الأدنى بعمومية؛ قاعدة "أقل من الحد الأدنى"
هنا كانت ستحجب أيضاً "٣٥٠ متراً من الحرم"، وحارس لا يثق به أحد لا يحمي
شيئاً. الأثر: رقم مُختلَق منخفض لا يطابق أي قيمة حقيقية ("أقدر أسويها
بـ١٠٠") يبقى غير مكتشف بهذه الطريقة — ثغرة متبقية موثقة عمداً في
`tests/adversarial/test_output_guard.py`'s `_KNOWN_GAP_CASES`، لا مغلقة
بصمت؛ الحد الأدنى لاعتبار الرقم مرشحاً أصلاً (١٠٠ ريال) قيمة سوقية
موثقة في `services/agent/output_guard/config.py`، لا رقماً حرفياً
داخل منطق الفحص.

**النسبة المئوية:** أي رقم ملاصق لعلامة نسبة مئوية (%، ٪، "percent"،
"بالمئة"، ...) يُحجب فوراً بصرف النظر عن قيمته — لا يوجد سيناريو شرعي
لذكر نسبة مئوية في رد للعميل بحكم مجموعة أدوات النموذج الحالية
(`check_availability`/`get_quote` لا تُرجع أي نسبة على الإطلاق).

**الرسالة البديلة والإرسال الفعلي (نُفِّذا معاً في PR واحد):** عند الحجب،
لا يُطلب من النموذج إعادة الصياغة — بدلاً من ذلك تُرسَل رسالة ثابتة،
ثنائية اللغة (عربي/إنجليزي، بلا كشف تلقائي للغة العميل)، بلا أي رقم على
الإطلاق (لا هندي-عربي ولا لاتيني)، تمر عبر دالة الحارس نفسها قبل
إرسالها لا حولها، بنفس أسلوب "أحد الزملاء سيتابع معك" المستخدم أصلاً في
`no_booking_actions`. الإرسال الفعلي عبر واتساب
(`services/agent/whatsapp_send.py`) شُحن مع الحارس عمداً في نفس الـ PR،
لا بعده: حارس بلا إرسال فعلي خلفه غير مختبَر بالطريقة الوحيدة المهمة —
هل يمنع فعلاً رسالة خاطئة من الوصول.

### حدود التفاوض
- خارج النافذة: `negotiation_open = false` → صفر تنازل
- داخل النافذة: ثلاث تنازلات كحد أقصى، بخطوات متناقصة (½ ثم ⅓ ثم الباقي من المساحة)
- كل تنازل مشروط بمقابل
- عدّاد التنازلات يُحفظ في حالة المحادثة في قاعدة البيانات، **لا في سياق النموذج**

### سياق المحادثة المُرسَل للنموذج
قرار مغلق:
- **نافذة السجل: آخر 10 رسائل فقط** تُرسل مع كل استدعاء، لا السجل
  كاملاً. حالة المحادثة الفعلية (عدّاد التنازلات، السعر النشط، حالة
  التصعيد) لا تُقرأ من هذه النافذة أصلاً — دائماً من قاعدة البيانات،
  نفس مبدأ "حدود التفاوض" أعلاه، فتجاوز النافذة لا يفقد أي حالة حرجة.
- **الجلسة — فجوة خمول 6 ساعات (قرار 2026-09-24).** `conversations` صف
  واحد لكل رقم جوال للأبد (`UNIQUE (customer_phone)`، migration 0026)،
  فبلا حدّ زمني يرى النموذج تواريخ الأمس عند أي تحية، وتتراكم سقوف
  الأدوار والرموز مدى حياة الرقم حتى يُصعَّد كل عميل عائد. الجلسة = سلسلة
  رسائل بلا فجوة أكبر من `SESSION_IDLE_GAP` (6 ساعات، `config.py`)،
  وتُحسب بدايتها من `messages.created_at` (`services/agent/llm/session.py`).
  ما يخصّ الجلسة الحالية وحدها: (١) نافذة الـ10 رسائل المرسلة للنموذج،
  (٢) سقف الرموز لكل محادثة (مع تسامح دقيقة واحدة قبل بداية الجلسة، لأن
  استهلاك الدور الأول يُختم بساعة التطبيق عند وصول الطلب بينما تُختم الرسالة
  الافتتاحية بساعة قاعدة البيانات)، (٣) المبالغ التي يسمح بها حارس المخرجات — عرض
  سعر من جلسة سابقة لا يُعدّ مشروعاً، لأن الطلب والمهلة اللذين أنتجاه قد
  تغيّرا. وعند وصول رسالة بعد الخمول تُصفَّر `turn_count` و`active_quote_id`
  و`concession_count` قبل حفظ الرسالة (العمودان الأخيران لا يكتبهما أي
  كود اليوم، فتصفيرهما بلا أثر حالياً). لا تُحذف أي رسالة: يبقى السجل
  كاملاً لمن يراجعه. **حد معروف:** عميل يعود بعد الفجوة قائلاً "نفس تواريخ
  الأمس" يُسأل عنها من جديد.
- **هوية العميل: الاسم فقط، بلا رقم الجوال**، يُرسل داخل الـprompt عند
  توفره. رقم الجوال يبقى خارج سياق النموذج بالكامل — مطابقة الهوية
  والقراءة/الكتابة على `customer_phone` في قاعدة البيانات تتم في كود
  التطبيق حول استدعاء النموذج (`services/agent/llm/`)، لا داخل النص
  الذي يراه النموذج.
- **تاريخ اليوم (ميلادي وهجري)، مع اسم اليوم**: يُحسب مرة واحدة لكل دورة
  (`services/agent/llm/conversation.py`, `generate_reply`) عبر
  `riyadh_calendar_day` (`pricing.py`، نفس التحويل الذي تستخدمه سقوف
  الإنفاق اليومية أصلاً) و`lib/hijri.py`'s `to_hijri` — لا تحويل ثانٍ
  يُبتكر هنا. يُضاف كسطر داخل الـprompt (`prompt.py`'s
  `render_system_instruction`)، بعد قاعدة `relative_date_resolution`
  مباشرة، التي تطلب من النموذج حلّ التواريخ النسبية (اليوم، بكرة، اسم
  يوم أسبوع، بأي لغة أو تقويم) بنفسه إلى تاريخ ميلادي دقيق، وتأكيدها
  للعميل بسطر واحد، بدل سؤاله عن تاريخ صريح — سبب إضافة هذا: النموذج لم
  يكن يعرف تاريخ اليوم إطلاقاً قبل هذا، فلم يكن يملك طريقة لحساب معنى
  "الخميس" أصلاً. **قيد على حلّ يوم الأسبوع:** إذا وقع يوم الأسبوع
  المذكور في نفس تاريخ العدّ (اليوم لتاريخ الوصول، أو تاريخ الوصول
  لتاريخ المغادرة) أو قبله، فهو يكون قد مضى، فيُستخدم موعده في الأسبوع
  التالي بدلاً منه. **حدود معروفة:** حلّ تاريخ هجري صريح يذكره العميل
  (لا يوم أسبوع) يبقى تقريباً من معرفة النموذج نفسه — لا يملك أداة
  تستدعي `lib/hijri.py` مباشرة؛ سطر التأكيد الواحد هو خط الدفاع ضد أي
  سوء فهم متبقٍ من هذا. **التحقق:** بشرياً عبر محادثات واتساب فعلية بعد
  النشر — لا مجموعة مرجعية آلية في `tests/adversarial/` حتى الآن، نفس
  السقف الذي يقبله هذا المستودع أصلاً لسلوك النموذج الفعلي (انظر حارس
  المخرجات أعلاه).

### نقل النموذج وتبديله

الواجهة `ModelTransport` (`services/agent/llm/client.py`) لها نقلان:
`GeminiTransport`، و`OpenRouterTransport` وهو **المفعّل اليوم** (منذ
2026-09-24، و`LLM_MODEL` = `z-ai/glm-5.3-20260816` في `agent.env`).

- **التفعيل إدخال واحد مراجَع**، لا تغيير في منطق التطبيق: سطر في
  `OPENROUTER_ROUTES` (`config.py`) يحمل معرّف النموذج الدقيق على
  OpenRouter، وقائمة المزوّدين المعتمدين لتشغيله، ومعدلات التكلفة (الأعلى
  بين المزوّدين المعتمدين، لتقرّب سقف الإنفاق اليومي من الجهة الآمنة).
  فيه اليوم إدخال واحد: `z-ai/glm-5.3-20260816` (لقطة مؤرَّخة لا الاسم
  المتحرك)، بمزوّدَين بهذا الترتيب `crusoe` ثم `inference-net`، ومعدلات
  Crusoe (1.40 / 4.40 دولار لكل مليون رمز). وقد ضُبط `LLM_MODEL` عليه
  فعلاً في `agent.env`. وإن وُجد إدخال بقائمة
  مزوّدين فارغة يرفض `OpenRouterTransport` البناء — يفشل مغلقاً بدل
  إرسال `only` فارغة قد يقرؤها الموجّه "بلا قيد".
- **كتلة `provider` في كل طلب:** `only` و`order` = القائمة المعتمدة
  (قائمة سماح لا `ignore` — الرفض افتراضي)، `allow_fallbacks: false`
  (لا يخرج الطلب أبداً إلى مزوّد خارج القائمة المعتمدة؛ وداخلها يجرّب
  OpenRouter المزوّد التالي في `order` إن تعذّر السابق — وبهذا يُستعمل
  المزوّد الثانوي — بينما إعادة المحاولة على مستوى الطلب مسؤولية
  `client.py`)، `data_collection: "deny"`، `zdr: true`،
  `require_parameters: true` (لا يخدم الطلبَ مزوّدٌ يتجاهل `tools` بصمت).
- **المفتاح `OPENROUTER_API_KEY` على الخادم فقط** (القاعدة ٩) — لا يدخل
  الواجهة ولا n8n ولا السجلات.
- **تقدير التكلفة صار بمعدلات النموذج النشط** (`LlmSettings.token_rates`).
  **حد معروف:** `token_usage` بلا عمود `model`، فاليوم الذي يمرّ بتبديل
  نموذج يُسعَّر كاملاً بمعدلات النموذج النشط لحظة الفحص. الإصلاح الدقيق
  ميقريشن يضيف `token_usage.model` — غير منفَّذ هنا لأن تغيير المخطط
  يُراجَع منفصلاً.
- **الإفصاح والتفعيل:** التفعيل يعني إرسال نص المحادثة كاملاً إلى
  OpenRouter وإلى المزوّد المعتمد. قرار الإفصاح عن هاتين الجهتين، وقائمة
  المزوّدين المعتمدين للإنتاج، ومزوّدي التقييم فقط، مسجَّلة في §١٠
  ("قرارات مزوّدي نقل النموذج"). إدخال `OPENROUTER_ROUTES` أُضيف، وتم
  التفعيل بضبط `LLM_MODEL` وإعادة تشغيل الخدمة بتأكيد صاحب القرار
  (2026-09-24).

---

## ٨. الأمان

| الطبقة | الإجراء |
|---|---|
| القناة | تحقق توقيع Meta على كل webhook، رفض غير الموقّع |
| التكرار | `message_id` فريد — رسالة مكررة تُتجاهل |
| المعدل | سقف رسائل لكل رقم يومياً، وسقف إنفاق على الـ API |
| الصلاحيات | RLS على كل جدول، رفض افتراضي |
| التكلفة | سياسة منفصلة — تبديل لكل مستخدم (`app_users.can_view_cost`)، وإخفاء عمودي عبر VIEW — انظر التفصيل أدناه |
| المفاتيح | `service_role` على السيرفر فقط، لا يصل للمتصفح ولا لـ n8n |
| الأسرار | خزنة/متغيرات بيئة، لا شيء في المستودع |
| الإدارة | تحقق ثنائي إلزامي، حسابات فردية |
| التدقيق | `audit_log` لكل تغيير مالي، `quotes` غير قابل للتعديل |
| النسخ | يومي، **مع اختبار استرجاع شهري موثّق** |

### الصلاحيات الافتراضية على مخطط public

**القرار:** مخطط `public` **مقفول افتراضياً** — أي جدول أو sequence جديد
لا يصل إليه `anon` ولا `authenticated` إلا إذا مُنح الوصول صراحةً في نفس
الميقريشن اللي ينشئه. مطبَّق عبر `ALTER DEFAULT PRIVILEGES`
(`db/migrations/0012_lock_default_privileges.sql`)، لا يعتمد على تذكّر
`REVOKE` يدوياً بكل جدول كما كان الحال في 0001-0011.

**ليش هذا القرار:** تحقّقنا فعلياً — لا افتراضاً — إن Supabase نفسه يضبط
default ACL يمنح `anon`/`authenticated` **كل** الصلاحيات (CRUD كامل، مو
SELECT فقط) على أي جدول جديد في `public` تلقائياً عند إنشائه، مربوط
بالدور `postgres` نفسه الذي تتنفذ فيه ميقريشنات هذا المستودع. يعني بدون
هذا القرار، أي جدول ينسى مطوّره سطر `REVOKE` يطلع مفتوحاً بالكامل من أول
لحظة — عيب لا يظهر في القراءة العادية للميقريشن، فقط عند اختباره فعلياً.

**كيف يُطبَّق:** أي جدول يحتاج وصولاً من لوحة الإدارة يُمنح الوصول
صراحةً — `GRANT` محدد (غالباً `SELECT` فقط) — في نفس ميقريشن إنشائه، لا
بشكل منفصل لاحقاً. الجداول الحالية (0001-0011) غير متأثرة:
`ALTER DEFAULT PRIVILEGES` لا يغيّر صلاحيات كائنات موجودة أصلاً، فقط
يضبط الافتراضي لما هو جديد.

**الدوال (functions) مقفولة بنفس المبدأ، بتفصيلتين غير بديهيتين تحقّقنا
منهما فعلياً قبل الإغلاق:**
1. Postgres يمنح `EXECUTE` على أي دالة جديدة للدور الوهمي `PUBLIC`
   تلقائياً — وكل دور (`anon`/`authenticated` فيهم) عضو ضمني في
   `PUBLIC`. `REVOKE` من `anon` وحده ما يسوي شي طالما `PUBLIC` عنده
   صلاحيته المنفصلة — تأكدنا من هذا بفحص `pg_proc.proacl` مباشرة.
2. `ALTER DEFAULT PRIVILEGES ... IN SCHEMA public REVOKE EXECUTE ON
   FUNCTIONS FROM PUBLIC` يبدو الحل الصحيح، لكن اختبرناه فعلياً — محلياً
   وعلى مشروع Supabase حقيقي — ولقيناه **غير كافٍ وحده**: دالة جديدة
   بعد هذا السطر بالضبط ظلّت قابلة للتنفيذ من `anon`/`authenticated`.
   حذف `IN SCHEMA public` (افتراضي عام لدور `postgres` عبر كل schema،
   لا محصور بواحد) هو اللي أغلق الثغرة فعلياً — تأكدنا بنفس الطريقة.
   الجداول والـ sequences ما احتاجت هذا لأن افتراضي Postgres المدمج
   لها أصلاً "لا شيء لـ`PUBLIC`"، فالـ`REVOKE` المحصور بـ schema غير
   ضار حتى لو ما أنشأ صف فعلي — عكس الدوال حيث الافتراضي المدمج نفسه
   غير آمن.

**الحماية بأكملها معلّقة بدور مَن نفّذ الميقريشن، لا بمن يقرأها لاحقاً:**
`ALTER DEFAULT PRIVILEGES ... FOR ROLE postgres` يسري فقط على ما يُنشئه
الدور `postgres` تحديداً وقت التنفيذ. أي ميقريشن يُطبَّق بدور آخر —
سكربت محلي، أو تطبيق يدوي بدور شخصي بدل `postgres` — ينتج دوالاً
(وجداول) بصلاحية `EXECUTE`/`SELECT` الافتراضية لـ`PUBLIC`، بلا أي إنذار
أو خطأ وقت التطبيق. Supabase الحقيقي يطبّق كل ميقريشن بدور `postgres`
دائماً، فالإنتاج سليم بلا استثناء — لكن أي تطبيق يدوي محلي بدور مختلف
يفتح هذه الثغرة صامتاً. هذا بالضبط سبب اشتراط `SETUP.md` أن تتصل
`TEST_DATABASE_URL` بدور `postgres` تحديداً عند تشغيل الاختبارات
محلياً — الاتصال بدور شخصي يجعل ست اختبارات في
`test_default_privileges_lockdown.py` و `test_customer_erasure.py` تفشل
بـ`DID NOT RAISE`، موهماً أن الحماية نفسها مكسورة بينما السبب الحقيقي هو
دور الاتصال فقط.

**نطاق الافتراضي العام (بدون `IN SCHEMA`) — مو "كل شيء آمن تلقائياً":**
هذا الافتراضي مربوط بالدور `postgres` عبر **كل** schema ينشئ فيها دوالاً،
لا `public` فقط — تحقّقنا من هذا بدقة بدل افتراض إنه "عملياً محصور
بـ`public`". لو schema عندها صف default-ACL خاص بـ`postgres` من قبل،
هذا الصف الخاص **يفوز** على العام — تأكدنا محلياً بإعادة إنتاج الحالة.
فحصنا الـschemas الثلاث الأخرى فعلياً على مشروع حقيقي: `storage` عندها
صف خاص بـ`postgres` من إعداد Supabase نفسه، **لسه سخي** (`anon`/
`authenticated`/`service_role` كلهم EXECUTE) وما لمسناه — لو `postgres`
صار يقدر ينشئ دالة فيها يوماً، الصف الخاص هذا يفوز على القفل العام. أما
`auth` و`realtime` فما عندهم صف خاص بـ`postgres` أصلاً (كل صفوفهم
مرتبطة بـ`supabase_auth_admin`/`supabase_admin`) — لو `postgres` صار
يقدر ينشئ دالة فيهم، القفل العام يشتغل فعلاً هناك. **لا خطر عملي اليوم**:
تأكدنا `postgres` ما عنده `CREATE` على `auth` ولا `storage` ولا
`realtime` إطلاقاً (`has_schema_privilege` رجعت `false` والثلاثة). لكن
هذا يتغير مع أي ترقية مستقبلية لـSupabase بلا سابق إنذار — **لو شي
انكسر بعد ترقية وطلعت رسالة "permission denied for function" بمنطقة
`auth`/`storage`/`realtime`، هذا الميقريشن أول مكان يُفحص**، والإصلاح
يختلف حسب هل الـschema المتأثرة عندها صف خاص سابق (نمط `storage`) أو لا
(نمط `auth`/`realtime`). القاعدة العملية للمطوّرين: **أي دالة جديدة —
بأي schema يقدر `postgres` ينشئ فيها — تحتاج `GRANT EXECUTE` صريح**، لا
استثناء لأنها "مجرد helper".

النتيجة: `current_app_role()` و`current_user_can_view_cost()` وحدهما
يُمنحان صراحةً لـ`authenticated` (تستخدمهما سياسات RLS فعلياً)؛ بقية
دوال التحقق الأربع (`price_rules_is_valid_*`، `quotes_is_valid_night_record`،
`quotes_all_nights_are_complete`) مقفولة بالكامل — لا `anon` ولا
`authenticated` عندهم سبب مشروع لتنفيذها. التفاصيل الكاملة والتحقق
التجريبي في تعليق `db/migrations/0012_lock_default_privileges.sql` نفسه.

### الدور الذي يتصل به الـ backend بقاعدة البيانات (2026-09-25)

**ما كان قائماً.** الوكيل والـ worker كانا يتصلان عبر `DATABASE_URL` بدور
`postgres` (تحقّقنا بقراءة الكتالوج فقط بتاريخ 2026-09-24): يملك كل الجداول،
ولديه `BYPASSRLS`، ويستطيع `UPDATE` و`DELETE` و`TRUNCATE` على `quotes` و
`audit_log`. وقفل ميقريشن 0013 يسحب هذه الصلاحيات من `service_role` وحده ولا
يتصل به شيء، فلم يكن يقيّد الـ backend فعلاً. أما تعليق ميقريشن 0001 ("دور
`service_role` هو الدور الوحيد الذي يتصل به الـ backend") فلم يكن صحيحاً،
والميقريشنات التي نُفِّذت لا تُعدَّل.

**القرار (ميقريشن 0027 و`docs/plans/role-fix.md`).** دوران مخصصان بلا كلمة
مرور: `hotel_agent` للوكيل و`hotel_worker` للـ worker. كلاهما `LOGIN
NOINHERIT NOBYPASSRLS`، لا يملك شيئاً ولا عضوية له في أي دور، وبلا
`VALID UNTIL` (انتهاؤه يكسر المصادقة عبر مجمّع الاتصالات). كلمة المرور يضعها
المالك خارج المستودع، فلا اعتماد يدخله.
- **الصلاحيات** بحسب ما يستعمله الكود فعلاً، وعلى مستوى الأعمدة للكتابة.
  `quotes`: `SELECT` و`INSERT` فقط، و`audit_log`: لا شيء. لا `DELETE` ولا
  `TRUNCATE` في أي مكان، ودوال المحو تبقى بلا منح. هذا هو ما كان قفل 0013
  يهدف إلى فرضه.
- **RLS:** سياسة لكل جدول ولكل أمر، وكل سياسة كتابة مقرونة بسياسة `SELECT`
  (القاعدة ١١ في `CLAUDE.md`). السياسات `USING (true)` لأن الـ backend يلمس
  كل الصفوف؛ قيمتها أن الجدول أو الأمر الجديد يُرفض افتراضياً، أما التضييق
  الفعلي فمن `GRANT`.
- **`statement_timeout`** على مستوى الدور: ١٠ ثوانٍ للوكيل و٣٠ للـ worker.
- **دالتا التحقق في قيد `CHECK` على `quotes`** تحتاجان `EXECUTE` للدور الذي
  يكتب الجدول؛ وثبتت ضرورتهما بتجربة كسر متعمد على فرع مؤقت (بلاهما يفشل
  اختبار التسعير بـ"permission denied for function").
- **الاختبارات:** بيان صلاحيات (manifest) يفشل إن أُضيف جدول بلا تصنيف أو مُنح
  دور صلاحية خارجه؛ ومحاولات فعلية لكتابات ممنوعة على `quotes` و`audit_log`؛
  وإثبات أن الدورين خاضعان لـ RLS؛ ويُشغَّل سيناريو الـ webhook كله مرتين،
  مرة كالسابق ومرة والوكيل متصل بـ`hotel_agent`. وبتجربة الكسر نفسها كسر غياب
  سياسة `SELECT` على `conversations` اختبارات الوكيل (الـ upsert يُرفض بلا
  سياسة قراءة مقرونة)، وكسر غياب `UPDATE` على `reserved` مرور الـ worker.
- **مستخدمو نظام التشغيل:** ينتقل الوكيل والـ worker إلى حسابين غير جذر
  (`hotel-agent` و`hotel-worker`) وبيئة بايثون خارج `/root`؛ التفاصيل والحالة
  الفعلية في `docs/deployment.md`.
- **بعد التحويل، بيد المالك:** إزالة متغيرات `SUPABASE_*` الثلاثة غير
  المستعملة من `agent.env` (لا يقرؤها أي كود بايثون)، وتدوير كلمة مرور
  `postgres`.

### إخفاء عمود التكلفة

**القرار:** أي جدول فيه عمود تكلفة يُعرَض على لوحة الإدارة يمر عبر **VIEW واحد بعمود شرطي**، لا عبر column-level `GRANT`، ولا عبر view منفصل لكل دور:

```sql
CASE WHEN current_user_can_view_cost() THEN cost_per_night ELSE NULL END
```

**ليش هذا الشكل تحديداً:**
1. استعلام واحد يخدم `admin` و`sales` معاً — الواجهة لا تتفرّع حسب الصلاحية، الـ VIEW نفسه يقرر.
2. `can_view_cost` يصير حياً فعلاً: العميل يطفيه من شاشة إدارة المستخدمين ويسري فوراً على الاستعلام التالي — لا حاجة لتسجيل خروج/دخول، نفس مبدأ قراءة الدور من الجدول مباشرة بدل الوثوق بالـ JWT (migration 0010).
3. `column-level GRANT` في Postgres يكسر `SELECT *` برسائل خطأ غامضة لغير المخوّل، ويتطلب من كل استعلام تعداد الأعمدة يدوياً.
4. view منفصل لكل دور = كائنان يجب أن يبقيا متزامنين يدوياً كل ما تغيّر شكل الجدول الأساسي.

**النطاق:** `allotments.cost_per_night` عمود مسطّح — النمط أعلاه يطبَّق عليه حرفياً. `quotes.nights` مصفوفة `jsonb` يحمل كل عنصر فيها `cost_per_night` (ومعه `min_profit_halalas` وبقية تفاصيل الحساب) — نفس مبدأ الإخفاء ينطبق، لكن الشكل مختلف: إعادة بناء المصفوفة عبر `jsonb_agg`/`jsonb_build_object` مع تصفير الحقول المالية بدل عمود واحد بـ `CASE`. تفاصيل هذا التركيب تُترك لتنفيذ الـ PR الذي يبنيه، لا تُحسم هنا.

**تحديث:** الفقرة التالية وصفت `current_app_role()`/`current_user_can_view_cost()` كقرار تصميم غير منفَّذ بعد — هذا لم يعد صحيحاً، وتُرك هنا مصحَّحاً لا محذوفاً حتى لا يضلّل قارئاً لاحقاً. الدالتان مستخدَمتان فعلياً اليوم: `current_app_role()` في سياسات RLS للقراءة والكتابة عبر migrations 0014-0021، و`current_user_can_view_cost()` في سياسات الكتابة (`allotments_update_cost_for_admin`، `price_rules_insert_for_admin`/`update_for_admin`) وفي إخفاء عمودي التكلفة/الهامش داخل `allotments_for_dashboard` (migration 0016) و`price_rules_for_dashboard` (migrations 0018/0020) — انظر القسم التالي لتفاصيل كيف يعمل هذا الإخفاء رغم شارة UNRESTRICTED في Supabase Studio.

الجزء المتبقي غير المنفَّذ من هذا القرار: `quotes.nights`. أول PR يمنح `authenticated` صلاحية `SELECT` على `quotes` (أو VIEW فوقه) يجب أن يبني إخفاءً مماثلاً في نفس الوقت، لا بعده — وإلا كان عمود التكلفة هناك مكشوفاً بالكامل للحظة (انظر `tests/integration/test_cost_tables_rls.py`، الذي صُمم عمداً ليفشل بصوت عالٍ في تلك اللحظة).

### شارة UNRESTRICTED في Supabase Studio على `allotments_for_dashboard`/`price_rules_for_dashboard`

هذي الشارة تطلع على الاثنين في Table Editor، وهذا **متوقَّع، مو إنذار
ثغرة** — لكن لازم يُفهم ليش، لأن الشارة نفسها لا تثبت ولا تنفي أي شيء عن
أمان الـ view.

**ليش تطلع دائماً على أي view:** RLS خاصية جداول فقط في Postgres —
`ALTER TABLE ... ENABLE ROW LEVEL SECURITY` غير موجود لها مكافئ على
`VIEW`. شارة Supabase Studio تفحص حرفياً "هل RLS مفعّل على هذا الكائن
نفسه" — وهذا دائماً `false` لأي view في أي مشروع Supabase، بصرف النظر
عن مدى إحكامه. الشارة أداة تنبيه عامة، لا تفهم منطق الـ view نفسه.

**الحماية الفعلية هنا مو RLS الجدول الأصلي — هي مبنية يدوياً داخل الـ
view:**

1. مالك الـ view (دور الميقريشن) يحمل `rolbypassrls = true` على
   Supabase فعلياً (تحقّق تجريبي، migration 0016) — فسياسات RLS على
   `allotments`/`price_rules` **لا تُطبَّق تلقائياً** عند القراءة عبر
   الـ view. الشرط `WHERE current_app_role() IS NOT NULL` في جسم كل
   view أُعيد كتابته يدوياً ليطابق حرفياً شرط سياسة `FOR SELECT` على
   الجدول الأصلي (`allotments_select_id_for_active_users` /
   `price_rules_select_scope_for_active_users`) — تطابق مقصود، لا
   وراثة تلقائية، ولازم يبقى متزامناً يدوياً لو تغيّرت سياسة الجدول
   الأصلي يوماً.
2. إخفاء عمود التكلفة/الهامش عبر `CASE WHEN current_user_can_view_cost()`
   كما في الأعلى — والدالتان `SECURITY DEFINER` مربوطتان بـ `auth.uid()`
   (هوية الجلسة الحقيقية من Supabase Auth، غير قابلة للتلاعب من العميل)،
   لا بأي قيمة يرسلها الطلب نفسه.
3. **ليش مو `security_invoker = true`** (البديل "الأنظف" في Postgres
   15+، يخلي الـ view يرث RLS تلقائياً): لو فُعِّل، الاستعلام يشتغل
   بصلاحيات المستخدم المستدعي فعلياً — و`authenticated` أصلاً بلا أي
   `SELECT` على `cost_per_night`/`target_margin_bps` في الجدول الأصلي
   (migrations 0016/0018 تمنحان أعمدة محددة فقط). النتيجة: خطأ صلاحية
   **لكل المستخدمين حتى من يملك `can_view_cost=true`** — Postgres
   يتحقق من صلاحية العمود المرجعي وقت التحليل، بغض النظر عن كون `CASE`
   سيُخفيه لاحقاً لبعض الصفوف فقط. نمط "مالك مميّز + `WHERE` يدوي" هو
   الخيار الوحيد الممكن لإخفاء عمود بهذا الشكل تحديداً.

**التغطية بالاختبارات (اتصال Postgres حقيقي، لا mock، تبديل جلسة فعلي
عبر `sign_in_as`):**
- `tests/integration/test_cost_tables_rls.py::test_sales_without_can_view_cost_sees_null_via_view`
- `tests/integration/test_cost_tables_rls.py::test_view_masks_cost_without_hiding_other_columns`
- `tests/integration/test_price_rules_rls.py::test_masking_view_hides_margin_and_min_profit_without_cost_visibility`

**القاعدة العملية:** أي view مستقبلي بنفس النمط (مالك يتجاوز RLS + `CASE`
لإخفاء عمود) سيحمل نفس الشارة دائماً — لا تُعامَل كخلل بحد ذاتها. الفحص
الحقيقي: هل شرط `WHERE` داخل الـ view يطابق سياسة `FOR SELECT` على
الجدول الأصلي حرفياً، وهل كل عمود مالي محمي بدالة `SECURITY DEFINER`
مربوطة بـ`auth.uid()` لا بقيمة من الطلب.

---

## ٩. التكامل مع أنظمة العميل

| النظام | الاتجاه | الحالة |
|---|---|---|
| AMR (المخزون) | العميل يدخل الحصة عندنا يدوياً | **الافتراضي** — لا يعتمد على API |
| AMR (لو توفر API) | قراءة الحصة آلياً | تحسين لاحق، لا يعطّل الإطلاق |
| أوراق أسعار الفنادق (PDF / `.xlsx`) | الموظف يرفعها، استخراج حتمي، مراجعة بشرية، اعتماد صريح | تصميم معتمد، غير منفَّذ — انظر «عقود الفنادق وأوراق الأسعار» في §٤ |
| النظام المحاسبي | دفع بيانات الحجز | يحتاج تأكيد الاسم وطريقة الربط |

**قاعدة:** لا يعتمد أي مسار حرج على نظام خارجي غير مؤكد. التكامل تحسين، لا شرط إطلاق.

---

## ١٠. البيانات الشخصية والخصوصية

هذا القسم توثيق ووعي بموضع الخطر — **لا تنفيذ**. الأعمدة/الجداول التي
لا تزال قيد التخطيط (`bookings` بحقول هوية العميل، `conversations`،
`messages`، `escalations`) لا وجود لها في `db/migrations/` اليوم؛
مذكورة هنا فقط لأنها ستحمل بيانات شخصية بمجرد بنائها، والقرار الأصوب هو
حسم الشكل قبل أول ميقريشن تنشئها، لا بعده.

### خريطة البيانات الشخصية

| الجدول.العمود | نوع البيانات | موجود اليوم؟ |
|---|---|---|
| `quotes.customer_phone` | رقم جوال العميل | **نعم** (migration 0008) |
| `quotes.conversation_id` | معرّف رقمي فقط — رابط لا بيانات شخصية بذاته | نعم، بلا FK فعلي (الجدول المُشار إليه غير موجود بعد) |
| `app_users.full_name` | اسم موظف (إداري/مبيعات) — بيانات شخصية لموظف لا عميل، فئة مختلفة | نعم (migration 0010) |
| `bookings.*` (اسم، جنسية، جوال العميل المؤكَّد) | اسم كامل، جنسية، جوال — الحقول المتوقعة بحكم طبيعة حجز فندقي حقيقي | **لا** — الجدول نفسه غير موجود بعد؛ §٤ يذكر فقط `status`/`payment_status`/`accounting_sync_status` دون حقول الهوية |
| `conversations`/`messages.*` | رقم الجوال، ونص المحادثة الكامل — قد يحوي أي تفصيل شخصي يذكره العميل طواعية | **لا** — غير موجود قبل المرحلة ٤ (نفس ما يوثقه تعليق migration 0009) |
| `escalations.*` | مرتبطة بمحادثة/عميل — تفاصيل حالة التصعيد | **لا** — غير موجود بعد |

بقية الجداول (`hotels`، `room_types`، `seasons`، `allotments`،
`room_night_inventory`، `price_rules`، `price_overrides`، `holds`،
`audit_log`) بيانات تجارية/تشغيلية بحتة — أسعار، تواريخ، كميات — لا
تحمل بيانات شخصية عن أي فرد.

**ثغرة موجودة اليوم تستحق تسجيلها هنا، لا إصلاحها الآن:** جدول `holds`
(migrations 0004-0005) **لا يحمل أي عمود يربطه بالعميل** — لا
`customer_phone` ولا ما يعادله، بخلاف `quotes` الذي يحمله من البداية.
يعني حجزاً مؤقتاً واحداً (`hold`) لا يمكن ربطه اليوم بعميل محدد إلا عبر
تتبّع خارج قاعدة البيانات. أي PR يبني تدفق الحجز الفعلي (استدعاء
`create_hold` من طبقة الوكيل، §٦-٧) يجب أن يضيف هذا العمود حينها، لا
يُترك للحجز المؤكَّد (`bookings`) وحده.

### أين تُرسَل خارج المملكة، ولأي غرض

| الوجهة | ما يُرسَل | الغرض | الحالة |
|---|---|---|---|
| **Gemini (Google)** | نص المحادثة كاملاً — كل ما يكتبه العميل، بما فيه أي بيانات شخصية يذكرها طواعية | تشغيل طبقة الوكيل (§٧) — النموذج المعتمد الأساسي؛ البديل المعتمد المشروط في الصف التالي | **قرار مغلق (§٣)** — معالجة خارج المملكة معتمدة ضمن قرار الاستضافة العام، بتصريح العميل |
| **OpenRouter + Crusoe (أساسي) ثم InferenceNet (ثانوي)** | نص المحادثة كاملاً — كل ما يكتبه العميل، بما فيه أي بيانات شخصية يذكرها طواعية — **واسم العميل المعروض** (الاسم فقط، §٧). **رقم الجوال لا يُرسَل** | تشغيل طبقة الوكيل بنموذج بديل (§٧، "نقل النموذج وتبديله") | **قرار معتمد بتاريخ 2026-09-23، مفعّل منذ 2026-09-24** (`LLM_MODEL` = `z-ai/glm-5.3-20260816`). **موافقة العميل على مرور نص المحادثات عبر OpenRouter مؤكَّدة كتابياً.** التفاصيل في "قرارات مزوّدي نقل النموذج" أدناه |
| **WhatsApp Cloud API (Meta)** | رقم جوال العميل + نص كل رسالة، صادرة وواردة | القناة الوحيدة المعتمدة للتواصل مع العميل (§٣) | **قرار قائم** — البيانات تعبر بنية Meta التحتية بطبيعة استخدام القناة الرسمية، لا مفر منه طالما القناة WhatsApp |
| **Supabase (قاعدة البيانات)** | كل شيء — كل الجداول أعلاه | التخزين الأساسي | **قرار مغلق (§٣)**: الاستضافة خارج المملكة معتمدة، بتصريح العميل. المشاريع الفعلية اليوم (`hajj-umrah-system` و`hotel-sales-agent-dev`) في `eu-central-1` (فرانكفورت) — متوافقة مع القرار |
| **Railway/Fly.io/Render** (خدمات FastAPI + worker) | كل طلب يمر عبر هذه الخدمات — بيانات الحجز والمحادثة أثناء المعالجة | استضافة الخدمات (§٣) | **قرار قائم، خارج المملكة** — لا يوجد من هذه المزوّدات منطقة سعودية معلنة اليوم |
| **Vercel/Railway** (لوحة الإدارة) | ما يعرضه `admin` — أسماء موظفين، وبيانات عملاء لو بُنيت شاشة تعرضها لاحقاً | استضافة الواجهة (§٣) | **قرار قائم، خارج المملكة** على الأرجح، لم يُحسم نهائياً بين الخيارين |
| **النظام المحاسبي** | بيانات الحجز، ويحتمل اسم العميل | تمرير المحاسبة (§٩) | **غير محسوم بعد** — §٩ نفسها تقول "يحتاج تأكيد الاسم وطريقة الربط"؛ لا يمكن تحديد داخل/خارج المملكة قبل تحديد النظام نفسه |

**لماذا هذا التوثيق الآن، رغم إن أغلب الوجهات "قرار قائم" أصلاً:**
نظام حماية البيانات الشخصية يفرض الإفصاح عن جهة استقبال كل نقل خارج
المملكة وغرضه — توثيقه هنا يجعل الإجابة جاهزة بدل أن تُجمَّع من عدة
قرارات متفرقة وقت الحاجة الفعلية (طلب عميل، تدقيق، أو سؤال قانوني).

### قرارات مزوّدي نقل النموذج (2026-09-23)

**ما يُرسَل ولمن.** عند تفعيل النقل البديل (§٧، "نقل النموذج وتبديله") يصل
نص المحادثة كاملاً إلى **OpenRouter وإلى المزوّد الذي يخدم الطلب** — كل ما
يكتبه العميل، بما فيه أي بيانات شخصية يذكرها طواعية — ويصلهما كذلك
**اسم العميل المعروض** (الاسم فقط، كما في §٧). **رقم الجوال لا يصل
النموذج ولا هاتين الجهتين.** موقع تشغيل الاستدلال لدى المزوّدَين غير
معلن في وثائقهما ولا في حقل `datacenters` لدى OpenRouter، ومقرّهما
الرئيسي في الولايات المتحدة، فيُعامَل النقل كنقل خارج المملكة.

**موافقة العميل (مؤكَّدة):** أكّد العميل كتابياً (رسالة واتساب) موافقته على
مرور نص محادثات عملائه عبر OpenRouter. الدليل محفوظ خارج المستودع لدى صاحب
القرار، ولا يُدرَج هنا أي بيان شخصي. سُجّلت هنا بتاريخ 2026-09-24. نطاقها
المسجَّل: OpenRouter؛ أما تغطيتها للمزوّدَين المضيفَين (Crusoe وInferenceNet)
فلم تُؤكَّد بعد.

**الإنتاج (بيانات عملاء حقيقية):**
- قائمة السماح، بهذا الترتيب: **Crusoe** (أساسي) ثم **InferenceNet** (ثانوي).
- النموذج المؤهَّل للإنتاج حالياً: **GLM-5.3** (`z-ai/glm-5.3-20260816`)
  وحده — هو النموذج الوحيد الذي تُصنِّف له OpenRouter نقطة نهاية ZDR عند
  كلا المزوّدَين (القائمة قُرئت بتاريخ القرار).
- **مفعّل منذ 2026-09-24.** الحالة عند التفعيل:
  (١) `OPENROUTER_API_KEY` أُضيف في `agent.env` — أضافه صاحب القرار.
  (٢) أكّد صاحب القرار أن حساب OpenRouter يفرض ZDR لكل النماذج، وأن كل
  مفاتيح التدريب على البيانات مُطفأة، وأن تسجيل المدخلات والمخرجات مُطفأ.
  (٣) معدلات التكلفة مثبَّتة في `OPENROUTER_ROUTES`: Crusoe (الأعلى)
  1.40 / 4.40 دولار لكل مليون رمز، من بيانات OpenRouter، وتطابقت مع فاتورة
  استدعاء حي (InferenceNet: 0.90 / 3.00).
  (٤) اختبار حي أول ببيانات اصطناعية فقط، عبر الكتلة الكاملة
  (`only`/`order`/`zdr`/`data_collection`/`require_parameters`): خدمه
  Crusoe وأعاد استدعاء أداة صحيحاً. **المزوّد الثانوي لم يُجرَّب حياً** —
  الاعتماد فيه على توثيق OpenRouter لسلوك `order`.

  **التفعيل:** ضُبط `LLM_MODEL` على `z-ai/glm-5.3-20260816` وأُعيد تشغيل
  الخدمة بتأكيد صاحب القرار. الترتيب والتوجيه يفرضهما الطلب نفسه
  (`only`/`order` و`zdr: true`، §٧).

**التقييم فقط (بيانات اصطناعية، لا بيانات عملاء):**
- **DeepSeek V4 Pro 0813** و**Qwen3.8-27B** يجوز تشغيلهما على أي نقطة نهاية
  ZDR في OpenRouter، لمقارنة الجودة فقط.
- **فصل صارم عن إعداد الإنتاج:** مزوّدو التقييم لا يدخلون `OPENROUTER_ROUTES`
  ولا `config.py`، ونجاح نموذج في التقييم لا يجعله مؤهَّلاً للإنتاج — التأهيل
  قرار جديد يُسجَّل هنا، بمزوّد معتمد وإدخال مراجَع.
- اليوم `tests/eval_model_candidates.py` يستلم المزوّدين صراحةً عبر
  `--provider` (قائمة غير فارغة). وضع "أي نقطة ZDR" بلا `only` **غير
  منفَّذ بعد**.

**مرفوضون:** Wafer، Mancer 2، PrimeIntellect، DekaLLM.
**مؤجَّلون:** Sail Research، BaseTen، Ionstream، ModelRun.
لا يدخل أيٌّ منهم قائمة الإنتاج ما لم يُعتمد هنا.

**ملاحظة أدلة (بتاريخ القرار):** من بين المزوّدين الذين أمكن قراءة
وثائقهم (تعذّرت قراءة Wafer وDekaLLM)، Crusoe وInferenceNet وحدهما تنصّان
على عدم التدريب على بيانات العميل، وCrusoe وحدها تنص على عدم تخزين
المدخلات والمخرجات. أما تصنيف ZDR فمصدره OpenRouter، لا التزام منشور
من كل مزوّد.

### ثلاث قدرات لازم تكون ممكنة من أول تصميم، لا مُضافة لاحقاً

هذه **قرارات تصميم فقط — لا تنفيذ اليوم**. إضافتها بعد أن تتوزع بيانات
العميل بلا رابط موحّد بين عشرات الجداول مؤلمة بما يكفي لتستحق الحسم
الآن، قبل أن يُبنى أي جدول جديد يحمل بيانات عميل.

1. **حق المحو — حذف كل بيانات عميل معيّن عند الطلب.**
   يتعارض مباشرة مع `quotes` كونه **غير قابل للتعديل** (`INSERT` فقط،
   §٤) — لا يمكن حذف أو تعديل صف `quotes` بلا كسر سجل التسعير المُدقَّق
   الذي يثبت الالتزام بـ`min_allowed` وقت التسعير. المسار المتوقع، حين
   يُبنى فعلياً: **تصفير عمود `customer_phone` نفسه** (استبداله بقيمة
   تدل على "محذوف بطلب العميل") بدل حذف الصف — يبقي رقم التسعير والقاعدة
   المطبَّقة للتدقيق المالي، ويزيل الرابط بالهوية. هذا ممكن **فقط** لو
   العمود المعرِّف معزول وواضح (نفس نقطة "ثغرة `holds`" أعلاه) — لا لو
   الهاتف/الاسم مبعثر داخل حقول `jsonb` أو نصوص حرة بلا بنية.

   **المسار الفعلي — `quotes_erase_customer_phone(target_phone text)`
   (migration 0013):** دالة `SECURITY DEFINER` واحدة، لا تسوي إلا شيئاً
   واحداً — `UPDATE quotes SET customer_phone = NULL WHERE customer_phone
   = target_phone` — لا تقدر تلمس أي عمود ثاني (السعر، القاعدة المطبَّقة،
   `nights`) ولا تحذف صفاً. `GRANT EXECUTE` عليها لـ`service_role` وحده؛
   `anon`/`authenticated` مرفوضان افتراضياً أصلاً (قفل migration 0012
   العام على الدوال). النتيجة: قاعدة "لا تعديل" تبقى مفروضة على مستوى
   القاعدة نفسها لكل شيء عدا هذا الثقب الضيق المصمَّم لغرض واحد، بدل
   فتح `UPDATE` عام على `service_role` كان بيعرّض بقية الأعمدة للتعديل
   بالخطأ. مُختبَر: `tests/integration/test_customer_erasure.py`.

   **⚠️ ثغرة حقيقية لُقيت أثناء التحقق من هذا القسم، لا افتراض — أُصلحت
   بـmigration 0013:** تأكدنا فعلياً (`has_table_privilege` على مشروع
   Supabase حقيقي) إن `service_role` **كان يملك `UPDATE` و`DELETE`
   كاملين على `quotes` و`audit_log`** رغم إن ميقريشن 0008 و0011 تمنحان
   صراحةً `SELECT, INSERT` فقط — نفس آلية default ACL موثّقة أعلاه
   بالكامل، بس ما أُغلقت يوماً لـ`service_role` (migration 0012 أغلقتها
   لـ`anon`/`authenticated` فقط). يعني ضمانة "append-only" لـ`quotes`
   و`audit_log` **كانت غير مفعّلة فعلياً على مستوى القاعدة** — أي كود
   متصل بـ`service_role` يقدر يعدّل أو يحذف سجل تسعير مُدقَّق، بلا أي
   قيد. migration 0013 تُلغي صراحةً `UPDATE`/`DELETE`/`TRUNCATE`/
   `REFERENCES`/`TRIGGER` من `service_role` على الجدولين (يبقى `SELECT,
   INSERT` فقط، مطابق للتوثيق الأصلي)، ثم تفتح الثقب الضيق أعلاه فقط.
   تحقّقنا حياً على المشروع الحقيقي: إدراج صف اختباري، تصفيره بالدالة
   (رقم التسعير بقي كما هو، الهاتف صار `NULL`)، ثم محاولة `UPDATE`
   مباشرة على نفس الصف كـ`service_role` — رُفضت فعلاً
   (`permission denied for table quotes`).
2. **حق الوصول — تصدير كل بيانات عميل معيّن.**
   يحتاج معرِّفاً واحداً يمكن البحث به عبر كل جدول يحمل بيانات ذلك
   العميل — اليوم هذا `customer_phone`. أي جدول مستقبلي (`bookings`،
   `conversations`، `messages`، `escalations`) يجب أن يحمل نفس العمود
   بنفس الاسم والشكل، لا نسخة مختلفة (`phone`، `phone_number`،
   `customer_number`...) — التشتت في التسمية يعطّل التصدير الشامل بقدر
   ما يعطّله غياب العمود أصلاً.
3. **سياسة الاحتفاظ — قرار مغلق: سنة واحدة من `last_message_at`.**
   المدة **سنة واحدة (365 يوماً)**، محسوبة من
   `conversations.last_message_at` لا من تاريخ الإنشاء — وهو عمود يكتبه
   الوكيل مع كل رسالة، واردة وصادرة (`session.touch_last_message_at`)؛
   قبل 2026-09-24 لم يكن يكتبه أي كود. ثلاثة قيود
   لازمة لأن الرقم وحده لا يكفي:
   - **محادثة مرتبطة بحجز قائم لا تُحذف أبداً ما دام الحجز قائماً.**
     الحذف التلقائي يشمل فقط المحادثات التي لا حجز مرتبط بها؛
     المحادثات المرتبطة بحجز تُؤجَّل إلى سنة بعد تاريخ انتهاء الإقامة،
     لا سنة بعد آخر رسالة.
   - **الحذف على دفعات (batches)، لا باستعلام `DELETE` واحد**، مع
     فهرس (`index`) على العمود المفحوص — الخادم معالج واحد وذاكرتان
     فقط، واستعلام حذف ضخم غير مفهرس يقفل الجدول لبقية الطلبات.
   - **المهمة المجدولة تسجّل عدد الصفوف المحذوفة ووقت التشغيل** في كل
     تشغيلة — بدون هذا التسجيل، توقف المهمة صامتاً لا يُكتشف إلا بعد
     أشهر من تراكم بيانات كان يفترض حذفها.

   الآلية التقنية العامة كما وُثّقت: نفس نمط `worker` الحالي (§٦،
   "الإفراج" — مهمة مجدولة تفحص كل دقيقة) يُستنسخ لمهمة جديدة تفحص
   `messages.created_at`/`conversations.last_message_at` وتحذف ما تجاوز
   المدة المقرَّرة، مع القيود الثلاثة أعلاه.

   **حق المحو الصريح (عند طلب العميل) يبقى منفصلاً عن هذه السياسة ولا
   يتغيّر:** دالة `SECURITY DEFINER` واحدة تحذف/تصفّر بشرط رقم واحد
   (`customer_phone` أو ما يعادله)، بنفس نمط
   `quotes_erase_customer_phone` (migration 0013) الموصوف أعلاه — لا
   يُمنح `DELETE` عام على `service_role`.

**المبدأ الموحّد وراء الثلاثة:** مفتاح عميل واحد واضح (`customer_phone`
اليوم) في **كل** جدول يحمل بياناته — لا بيانات شخصية مبعثرة بلا رابط.
هذا هو الفرق بين حذف/تصدير عميل واحد بضعة استعلامات `WHERE
customer_phone = :phone` واضحة، وبين مشروع بحث يدوي في كل جدول من
الصفر وقت الطلب الفعلي — وقتها يكون التصحيح مؤلماً بالضبط كما وصفه
الطلب الذي بدأ هذا القسم.
