# المعمارية — نظام المبيعات الآلي لشركة الفنادق

---

## ١. المبدأ الحاكم

> **نموذج اللغة طبقة محادثة فقط. لا يحسب سعراً، ولا يحجز غرفة، ولا يرى تكلفة.**

كل قرار يتعلق بالمال أو المخزون يُتخذ في خدمات حتمية مكتوبة بكود عادي، قابلة للاختبار، ونتيجتها ثابتة لنفس المدخلات.

النموذج يستدعي هذه الخدمات كأدوات، ويستقبل منها خيارات محدودة، ويتكلم بها فقط. وأي رقم ينطق به يُتحقق منه في السيرفر قبل التنفيذ.

**السبب:** حقن التعليمات هو الخطر الأول في وكيل يبيع للعامة. الرد الوحيد الفعّال هو ألا يملك النموذج الصلاحية أصلاً — لا أن نطلب منه ألا يستخدمها.

---

## ٢. المكوّنات

```
┌─────────────────────────────────────────────────┐
│  WhatsApp Cloud API  (رقم تجاري واحد)            │
└───────────────┬─────────────────────────────────┘
                │ webhook (موقّع)
┌───────────────▼─────────────────────────────────┐
│  agent-service        (FastAPI)                  │
│  • تحقق التوقيع + idempotency                    │
│  • حالة المحادثة                                 │
│  • استدعاء Gemini + tools                        │
│  • حارس المخرجات (output guard)                  │
└──┬──────────┬──────────┬───────────────┬─────────┘
   │          │          │               │
┌──▼────┐ ┌───▼─────┐ ┌──▼────────┐ ┌───▼────────┐
│pricing│ │inventory│ │ booking   │ │ escalation │
│service│ │ service │ │ service   │ │  service   │
└──┬────┘ └───┬─────┘ └──┬────────┘ └───┬────────┘
   └──────────┴──────────┴──────────────┘
                    │
        ┌───────────▼────────────┐
        │  Supabase / Postgres   │
        │  RLS على كل جدول        │
        └───────────┬────────────┘
                    │
   ┌────────────────┴─────────────────┐
   │                                   │
┌──▼──────────────┐          ┌────────▼─────────┐
│ admin-dashboard │          │  worker (cron)   │
│    (Next.js)    │          │ • إفراج المهل     │
└─────────────────┘          │ • التنبيهات       │
                             │ • تمرير المحاسبة  │
                             └──────────────────┘
```

---

## ٣. التقنيات

| الطبقة | الاختيار | السبب |
|---|---|---|
| قاعدة البيانات | Supabase / Postgres | RLS أصلية، معاملات قوية، خبرة سابقة |
| الخدمات | FastAPI (Python) | خبرة سابقة، اختبارات سهلة |
| الواجهة الإدارية | Next.js | خبرة سابقة |
| النموذج | Gemini | قرار معتمد — خلف واجهة قابلة للاستبدال |
| قناة المحادثة | WhatsApp Cloud API الرسمي | رقم تجاري واحد معتمد |
| المهام المجدولة | worker منفصل + pg_cron | إفراج المهل والتنبيهات |
| المستودع | GitHub | نسخ، مراجعة، تتبع |

### الاستضافة

| المكوّن | الاستضافة |
|---|---|
| قاعدة البيانات | Supabase (أقرب منطقة متاحة) |
| خدمات FastAPI + worker | Railway (أو Fly.io / Render) |
| الواجهة الإدارية | Vercel أو Railway |

**نقطة مفتوحة:** هل يشترط العميل بقاء البيانات داخل المملكة؟ إن نعم، يلزم استضافة ذاتية أو مزوّد محلي، ويتغير تقدير التكلفة والجهد.

### ما لا نستخدمه ولماذا
- **n8n**: للتنبيهات والتكاملات الجانبية فقط. **ممنوع** أن يحتوي أي منطق تسعير أو حجز — لأنه غير قابل للاختبار الآلي ولا للمراجعة في Git بشكل موثوق.
- **Redis**: غير ضروري بهذا الحجم. Postgres يكفي للأقفال والطوابير، وكل مكوّن إضافي نقطة فشل جديدة.

---

## ٤. نموذج البيانات

### الجداول الأساسية

**hotels** — الفنادق
`hotel_name` · `distance_to_haram_meters` · `star_rating` · `address_text` ·
`check_in_time` · `check_out_time` · `is_active`

**room_types** — أنواع الغرف لكل فندق
`hotel_id` · `room_type_name` · `capacity_adults` · `bed_configuration` · `size_sqm`

**hotel_amenities** — مرافق الفندق
`hotel_id` · `amenity` — صف لكل مرفق، والمفتاح الأساسي هو الزوج نفسه. لا يوجد
`UPDATE`: المرفق يُضاف أو يُحذف.

**قاعدة المحتوى:** كل ما يُخزَّن عن الفندق منظَّم — رقم، وقت، أو قيمة من قائمة
مغلقة يفرضها `CHECK`. لا يوجد عمود وصف حر عمداً: أي نثر يُخزَّن هنا يدخل سياق
النموذج، وحارس المخرجات (§٧) يتحقق من الأرقام المالية فقط — لا يستطيع أن يكشف
جملة تدّعي مرفقاً لا يملكه الفندق. توسيع قائمة المرافق ميقريشن بسطر واحد، وهذي
هي التكلفة المقصودة.

**الحقول التفصيلية تقبل `NULL`.** الميقريشن ٠٠٢٣ طُبِّق على صفوف موجودة،
والميقريشنات للأمام فقط، ولا توجد قيمة افتراضية صادقة لمسافة فندق حقيقي عن
الحرم. لذلك الوجود غير مفروض في SQL — القيود تفرض *صحة* القيمة لا وجودها.
اللوحة تُلزم بالحقول عند الحفظ وتُظهر شارة "ملف غير مكتمل" لما سبقها. **ربط ما
يعرضه الوكيل باكتمال الملف مسؤولية المرحلة ٤**، عند أول قراءة فعلية لهذي
الأعمدة — وبدونه تظل `search_alternatives` (§٧) بلا أساس تختار عليه.

**seasons** — المواسم — **يعرّفها العميل بالكامل من التقويم**
`name` · `calendar_type` (hijri | gregorian) · `start_month` · `start_day` · `end_month` · `end_day` · `priority` · `is_default`

**القواعد:**
1. البداية داخل، النهاية خارج.
2. عند التداخل يفوز الأعلى `priority` — والعميل هو من يحدد الأولوية. النظام لا يفترض أن الهجري أهم من الميلادي أو العكس.
3. **موسم افتراضي واحد إلزامي** (`is_default = true`) يغطي السنة كاملة بأولوية صفر. أي تاريخ لا يقع في موسم معرّف يرجع إليه.

**الأثر:** لا يمكن أن يوجد تاريخ بلا سعر. الفجوات في تعريف العميل تُغطى تلقائياً بالافتراضي بدل أن يفشل التسعير. هذا يزيل الحاجة لحسم الفجوات قبل الإطلاق.

**شاشة المواسم (phase 3 PR C، `admin/app/seasons/`):** محرر مواسم مع
معاينة تقويمية حية — تسمح للعميل يشوف أثر كل تعديل فوراً قبل الحفظ.
المعاينة تعيد تنفيذ منطق `season_contains`/`resolve_season_id`
(`services/pricing/seasons.py`) بلغة TypeScript
(`admin/lib/seasonCalendar.ts`) بدل استدعاء الخدمة حياً — لا يوجد اتصال
بين `admin/` وأي خدمة Python. القاعدة ٦ (تحويل هجري بوحدة واحدة فقط) لا
تُنتهك: كل حقيقة هجرية (طول الشهر، بداية الشهر ميلادياً) تأتي من جدول
مرجعي ثابت مولَّد مسبقاً (`admin/lib/hijriCalendarReference.json`، عبر
`tests/generate_hijri_calendar_reference.py`)، لا من حساب مباشر.

**ضمان التطابق بين الطرفين، مفروض في CI:** `tests/season_conformance_fixtures.json`
مولَّد من `season_contains`/`resolve_season_id` الحقيقيين
(`tests/generate_season_conformance_fixtures.py`)، ومُختبَر ضد
`seasonCalendar.ts` في `admin/lib/seasonCalendar.conformance.test.ts`
(Vitest). أي تعديل في أحد الطرفين بدون تحديث الآخر يكسر أحد الفحصين —
مُلزَم بهذا عشان معاينة المواسم ما تصير تكذب على العميل وهو يبني تعريفات
مواسمه عليها.

**allotments** — الحصة المخصصة
`hotel_id` · `room_type_id` · `date` · `total_rooms` · `cost_per_night`

**room_night_inventory** — الوحدة الذرية
`allotment_id` · `date` · `total` · `reserved` · `held`
```sql
CONSTRAINT inventory_never_oversold
  CHECK (reserved + held <= total AND reserved >= 0 AND held >= 0)
```
هذا القيد هو خط الدفاع الأخير ضد البيع المزدوج. لا يُزال ولا يُعطّل.

**price_rules** — قواعد التسعير
`scope` (global | season | hotel | room_type) · `scope_id` · `target_margin` · `min_profit_by_lead_time` (jsonb) · `demand_curve`

**quotes** — سجل غير قابل للتعديل
كل سعر نُطق به: التاريخ، رقم العميل، الليالي، سعر العرض، الحد الأدنى المسموح، القاعدة المنتِجة، معرّف المحادثة.
`INSERT` فقط — لا `UPDATE` ولا `DELETE`.

**holds** — الحجوزات المؤقتة
`expires_at` · `released_at` — يفرجها الـ worker تلقائياً

**bookings** — الحجوزات المؤكدة
`status` · `payment_status` · `accounting_sync_status`

**conversations** / **messages** — سجل المحادثات
**escalations** — حالات التصعيد وزمن الاستجابة
**audit_log** — كل تغيير في القيم المالية: من، متى، القيمة قبل وبعد

---

## ٥. خدمة التسعير

### الواجهة
```
POST /pricing/quote
IN : hotel_id, room_type_id, check_in, check_out, rooms
OUT: {
  ask_price_total,        # سعر العرض
  min_allowed_total,      # أدنى سعر مسموح به
  nights: [ {date, season_id, ask, min_allowed} ],
  negotiation_open: bool,
  quote_id
}
```

### قواعد صارمة
1. **لا تُرجع `cost` أبداً.** غير موجودة في الاستجابة، ولا في سياق النموذج.
2. التسعير **لكل ليلة** ثم يُجمع. الإقامة التي تعبر موسمين تُسعّر بموسمين.
3. `negotiation_open` تُحسب من المدة المتبقية للوصول فقط. خارج النافذة = `false`، والنموذج لا يملك أي مساحة.
4. الطلب يحرّك `ask_price` فقط. `min_allowed` لا يتأثر بالطلب إطلاقاً.
5. كل استدعاء يُسجَّل في `quotes` قبل الرد.

### تسلسل الحساب
```
cost
  → + target_margin (من قواعد التوريث: عام ← موسم ← فندق ← نوع غرفة)
  → × معامل الطلب (نسبة التصريف × المدة المتبقية)
  → = ask_price

cost
  → + min_profit (حسب المدة المتبقية والموسم)
  → = min_allowed

تجاوز يدوي إن وُجد ← يتقدم على الاثنين، لمدة محددة
```

---

## ٦. خدمة المخزون والحجز

### الحجز المؤقت
```
BEGIN;
  SELECT * FROM room_night_inventory
    WHERE allotment_id = ANY(...) AND date BETWEEN ...
    FOR UPDATE;                          -- قفل الصفوف

  -- التحقق من التوفر لكل ليلة
  UPDATE room_night_inventory SET held = held + :rooms ...;
  INSERT INTO holds (...) RETURNING id;
COMMIT;
```
لو انتهك القيد، تفشل المعاملة كاملة. **لا يوجد تحقق في طبقة التطبيق يُعتمد عليه وحده.**

**`idempotency_key`:** `create_hold` يتطلب مفتاحاً فريداً لكل طلب (عمود
`holds.idempotency_key`، `UNIQUE`) — إعادة نفس الطلب بنفس المفتاح ترجع
نفس الحجز بدل ما تنشئ حجزاً ثانياً. خدمة المخزون لا تولّد هذا المفتاح؛
المستدعي هو المسؤول عنه.

قرار للمرحلة ٤/٥ (طبقة الوكيل)، موثّق هنا عشان ما يُعاد اكتشافه وقت
البناء: المصدر هو `message_id` من واتساب مباشرة — فريد أصلاً حسب §٨،
فلا يحتاج تحويل. **إن صار عندنا أكثر من عملية idempotent على نفس
الرسالة مستقبلاً** (مثلاً `create_hold` و`confirm_booking` بنفس
المحادثة)، يتحول لـ namespace بدل الخام: `f"create_hold:{message_id}"`.

### مدة المهلة
| قبل الوصول | المهلة |
|---|---|
| > ١٥ يوم | ٤٨ ساعة |
| ٣ – ١٥ يوم | ١٢ ساعة |
| < ٤٨ ساعة | ساعتان + دفع كامل |

### التأكيد
`held → reserved` في معاملة واحدة، بعد تأكيد الدفع فقط.
**الكاش عند الوصول لا يؤكده الوكيل** — يفتح تصعيداً بشرياً.

### الإفراج
worker يعمل كل دقيقة: كل `hold` منتهٍ → `held -= rooms`, `released_at = now()`.

---

## ٧. طبقة الوكيل

### الأدوات المتاحة للنموذج
| الأداة | ما تفعله |
|---|---|
| `check_availability` | التوفر لتواريخ محددة |
| `get_quote` | سعر العرض والحد الأدنى المسموح |
| `create_hold` | حجز مؤقت |
| `confirm_booking` | تأكيد بعد الدفع |
| `escalate` | تحويل لبشر |
| `search_alternatives` | فنادق بديلة بنفس التواريخ |

**النموذج لا يملك أي أداة كتابة مباشرة على قاعدة البيانات.**

### حارس المخرجات
قبل إرسال أي رسالة للعميل، يمر النص على فحص برمجي:
1. استخراج أي أرقام مالية في النص
2. مطابقتها مع `quote` نشط في هذه المحادثة
3. رقم غير مطابق أو أقل من `min_allowed` → **الرسالة تُحجب**، ويُفتح تصعيد، ويُسجَّل الحادث

هذه آخر طبقة دفاع. تعمل حتى لو انهار النموذج تماماً.

### حدود التفاوض
- خارج النافذة: `negotiation_open = false` → صفر تنازل
- داخل النافذة: ثلاث تنازلات كحد أقصى، بخطوات متناقصة (½ ثم ⅓ ثم الباقي من المساحة)
- كل تنازل مشروط بمقابل
- عدّاد التنازلات يُحفظ في حالة المحادثة في قاعدة البيانات، **لا في سياق النموذج**

---

## ٨. الأمان

| الطبقة | الإجراء |
|---|---|
| القناة | تحقق توقيع Meta على كل webhook، رفض غير الموقّع |
| التكرار | `message_id` فريد — رسالة مكررة تُتجاهل |
| المعدل | سقف رسائل لكل رقم يومياً، وسقف إنفاق على الـ API |
| الصلاحيات | RLS على كل جدول، رفض افتراضي |
| التكلفة | سياسة منفصلة — تبديل لكل مستخدم (`app_users.can_view_cost`)، وإخفاء عمودي عبر VIEW — انظر التفصيل أدناه |
| المفاتيح | `service_role` على السيرفر فقط، لا يصل للمتصفح ولا لـ n8n |
| الأسرار | خزنة/متغيرات بيئة، لا شيء في المستودع |
| الإدارة | تحقق ثنائي إلزامي، حسابات فردية |
| التدقيق | `audit_log` لكل تغيير مالي، `quotes` غير قابل للتعديل |
| النسخ | يومي، **مع اختبار استرجاع شهري موثّق** |

### الصلاحيات الافتراضية على مخطط public

**القرار:** مخطط `public` **مقفول افتراضياً** — أي جدول أو sequence جديد
لا يصل إليه `anon` ولا `authenticated` إلا إذا مُنح الوصول صراحةً في نفس
الميقريشن اللي ينشئه. مطبَّق عبر `ALTER DEFAULT PRIVILEGES`
(`db/migrations/0012_lock_default_privileges.sql`)، لا يعتمد على تذكّر
`REVOKE` يدوياً بكل جدول كما كان الحال في 0001-0011.

**ليش هذا القرار:** تحقّقنا فعلياً — لا افتراضاً — إن Supabase نفسه يضبط
default ACL يمنح `anon`/`authenticated` **كل** الصلاحيات (CRUD كامل، مو
SELECT فقط) على أي جدول جديد في `public` تلقائياً عند إنشائه، مربوط
بالدور `postgres` نفسه الذي تتنفذ فيه ميقريشنات هذا المستودع. يعني بدون
هذا القرار، أي جدول ينسى مطوّره سطر `REVOKE` يطلع مفتوحاً بالكامل من أول
لحظة — عيب لا يظهر في القراءة العادية للميقريشن، فقط عند اختباره فعلياً.

**كيف يُطبَّق:** أي جدول يحتاج وصولاً من لوحة الإدارة يُمنح الوصول
صراحةً — `GRANT` محدد (غالباً `SELECT` فقط) — في نفس ميقريشن إنشائه، لا
بشكل منفصل لاحقاً. الجداول الحالية (0001-0011) غير متأثرة:
`ALTER DEFAULT PRIVILEGES` لا يغيّر صلاحيات كائنات موجودة أصلاً، فقط
يضبط الافتراضي لما هو جديد.

**الدوال (functions) مقفولة بنفس المبدأ، بتفصيلتين غير بديهيتين تحقّقنا
منهما فعلياً قبل الإغلاق:**
1. Postgres يمنح `EXECUTE` على أي دالة جديدة للدور الوهمي `PUBLIC`
   تلقائياً — وكل دور (`anon`/`authenticated` فيهم) عضو ضمني في
   `PUBLIC`. `REVOKE` من `anon` وحده ما يسوي شي طالما `PUBLIC` عنده
   صلاحيته المنفصلة — تأكدنا من هذا بفحص `pg_proc.proacl` مباشرة.
2. `ALTER DEFAULT PRIVILEGES ... IN SCHEMA public REVOKE EXECUTE ON
   FUNCTIONS FROM PUBLIC` يبدو الحل الصحيح، لكن اختبرناه فعلياً — محلياً
   وعلى مشروع Supabase حقيقي — ولقيناه **غير كافٍ وحده**: دالة جديدة
   بعد هذا السطر بالضبط ظلّت قابلة للتنفيذ من `anon`/`authenticated`.
   حذف `IN SCHEMA public` (افتراضي عام لدور `postgres` عبر كل schema،
   لا محصور بواحد) هو اللي أغلق الثغرة فعلياً — تأكدنا بنفس الطريقة.
   الجداول والـ sequences ما احتاجت هذا لأن افتراضي Postgres المدمج
   لها أصلاً "لا شيء لـ`PUBLIC`"، فالـ`REVOKE` المحصور بـ schema غير
   ضار حتى لو ما أنشأ صف فعلي — عكس الدوال حيث الافتراضي المدمج نفسه
   غير آمن.

**نطاق الافتراضي العام (بدون `IN SCHEMA`) — مو "كل شيء آمن تلقائياً":**
هذا الافتراضي مربوط بالدور `postgres` عبر **كل** schema ينشئ فيها دوالاً،
لا `public` فقط — تحقّقنا من هذا بدقة بدل افتراض إنه "عملياً محصور
بـ`public`". لو schema عندها صف default-ACL خاص بـ`postgres` من قبل،
هذا الصف الخاص **يفوز** على العام — تأكدنا محلياً بإعادة إنتاج الحالة.
فحصنا الـschemas الثلاث الأخرى فعلياً على مشروع حقيقي: `storage` عندها
صف خاص بـ`postgres` من إعداد Supabase نفسه، **لسه سخي** (`anon`/
`authenticated`/`service_role` كلهم EXECUTE) وما لمسناه — لو `postgres`
صار يقدر ينشئ دالة فيها يوماً، الصف الخاص هذا يفوز على القفل العام. أما
`auth` و`realtime` فما عندهم صف خاص بـ`postgres` أصلاً (كل صفوفهم
مرتبطة بـ`supabase_auth_admin`/`supabase_admin`) — لو `postgres` صار
يقدر ينشئ دالة فيهم، القفل العام يشتغل فعلاً هناك. **لا خطر عملي اليوم**:
تأكدنا `postgres` ما عنده `CREATE` على `auth` ولا `storage` ولا
`realtime` إطلاقاً (`has_schema_privilege` رجعت `false` والثلاثة). لكن
هذا يتغير مع أي ترقية مستقبلية لـSupabase بلا سابق إنذار — **لو شي
انكسر بعد ترقية وطلعت رسالة "permission denied for function" بمنطقة
`auth`/`storage`/`realtime`، هذا الميقريشن أول مكان يُفحص**، والإصلاح
يختلف حسب هل الـschema المتأثرة عندها صف خاص سابق (نمط `storage`) أو لا
(نمط `auth`/`realtime`). القاعدة العملية للمطوّرين: **أي دالة جديدة —
بأي schema يقدر `postgres` ينشئ فيها — تحتاج `GRANT EXECUTE` صريح**، لا
استثناء لأنها "مجرد helper".

النتيجة: `current_app_role()` و`current_user_can_view_cost()` وحدهما
يُمنحان صراحةً لـ`authenticated` (تستخدمهما سياسات RLS فعلياً)؛ بقية
دوال التحقق الأربع (`price_rules_is_valid_*`، `quotes_is_valid_night_record`،
`quotes_all_nights_are_complete`) مقفولة بالكامل — لا `anon` ولا
`authenticated` عندهم سبب مشروع لتنفيذها. التفاصيل الكاملة والتحقق
التجريبي في تعليق `db/migrations/0012_lock_default_privileges.sql` نفسه.

### إخفاء عمود التكلفة

**القرار:** أي جدول فيه عمود تكلفة يُعرَض على لوحة الإدارة يمر عبر **VIEW واحد بعمود شرطي**، لا عبر column-level `GRANT`، ولا عبر view منفصل لكل دور:

```sql
CASE WHEN current_user_can_view_cost() THEN cost_per_night ELSE NULL END
```

**ليش هذا الشكل تحديداً:**
1. استعلام واحد يخدم `admin` و`sales` معاً — الواجهة لا تتفرّع حسب الصلاحية، الـ VIEW نفسه يقرر.
2. `can_view_cost` يصير حياً فعلاً: العميل يطفيه من شاشة إدارة المستخدمين ويسري فوراً على الاستعلام التالي — لا حاجة لتسجيل خروج/دخول، نفس مبدأ قراءة الدور من الجدول مباشرة بدل الوثوق بالـ JWT (migration 0010).
3. `column-level GRANT` في Postgres يكسر `SELECT *` برسائل خطأ غامضة لغير المخوّل، ويتطلب من كل استعلام تعداد الأعمدة يدوياً.
4. view منفصل لكل دور = كائنان يجب أن يبقيا متزامنين يدوياً كل ما تغيّر شكل الجدول الأساسي.

**النطاق:** `allotments.cost_per_night` عمود مسطّح — النمط أعلاه يطبَّق عليه حرفياً. `quotes.nights` مصفوفة `jsonb` يحمل كل عنصر فيها `cost_per_night` (ومعه `min_profit_halalas` وبقية تفاصيل الحساب) — نفس مبدأ الإخفاء ينطبق، لكن الشكل مختلف: إعادة بناء المصفوفة عبر `jsonb_agg`/`jsonb_build_object` مع تصفير الحقول المالية بدل عمود واحد بـ `CASE`. تفاصيل هذا التركيب تُترك لتنفيذ الـ PR الذي يبنيه، لا تُحسم هنا.

**تحديث:** الفقرة التالية وصفت `current_app_role()`/`current_user_can_view_cost()` كقرار تصميم غير منفَّذ بعد — هذا لم يعد صحيحاً، وتُرك هنا مصحَّحاً لا محذوفاً حتى لا يضلّل قارئاً لاحقاً. الدالتان مستخدَمتان فعلياً اليوم: `current_app_role()` في سياسات RLS للقراءة والكتابة عبر migrations 0014-0021، و`current_user_can_view_cost()` في سياسات الكتابة (`allotments_update_cost_for_admin`، `price_rules_insert_for_admin`/`update_for_admin`) وفي إخفاء عمودي التكلفة/الهامش داخل `allotments_for_dashboard` (migration 0016) و`price_rules_for_dashboard` (migrations 0018/0020) — انظر القسم التالي لتفاصيل كيف يعمل هذا الإخفاء رغم شارة UNRESTRICTED في Supabase Studio.

الجزء المتبقي غير المنفَّذ من هذا القرار: `quotes.nights`. أول PR يمنح `authenticated` صلاحية `SELECT` على `quotes` (أو VIEW فوقه) يجب أن يبني إخفاءً مماثلاً في نفس الوقت، لا بعده — وإلا كان عمود التكلفة هناك مكشوفاً بالكامل للحظة (انظر `tests/integration/test_cost_tables_rls.py`، الذي صُمم عمداً ليفشل بصوت عالٍ في تلك اللحظة).

### شارة UNRESTRICTED في Supabase Studio على `allotments_for_dashboard`/`price_rules_for_dashboard`

هذي الشارة تطلع على الاثنين في Table Editor، وهذا **متوقَّع، مو إنذار
ثغرة** — لكن لازم يُفهم ليش، لأن الشارة نفسها لا تثبت ولا تنفي أي شيء عن
أمان الـ view.

**ليش تطلع دائماً على أي view:** RLS خاصية جداول فقط في Postgres —
`ALTER TABLE ... ENABLE ROW LEVEL SECURITY` غير موجود لها مكافئ على
`VIEW`. شارة Supabase Studio تفحص حرفياً "هل RLS مفعّل على هذا الكائن
نفسه" — وهذا دائماً `false` لأي view في أي مشروع Supabase، بصرف النظر
عن مدى إحكامه. الشارة أداة تنبيه عامة، لا تفهم منطق الـ view نفسه.

**الحماية الفعلية هنا مو RLS الجدول الأصلي — هي مبنية يدوياً داخل الـ
view:**

1. مالك الـ view (دور الميقريشن) يحمل `rolbypassrls = true` على
   Supabase فعلياً (تحقّق تجريبي، migration 0016) — فسياسات RLS على
   `allotments`/`price_rules` **لا تُطبَّق تلقائياً** عند القراءة عبر
   الـ view. الشرط `WHERE current_app_role() IS NOT NULL` في جسم كل
   view أُعيد كتابته يدوياً ليطابق حرفياً شرط سياسة `FOR SELECT` على
   الجدول الأصلي (`allotments_select_id_for_active_users` /
   `price_rules_select_scope_for_active_users`) — تطابق مقصود، لا
   وراثة تلقائية، ولازم يبقى متزامناً يدوياً لو تغيّرت سياسة الجدول
   الأصلي يوماً.
2. إخفاء عمود التكلفة/الهامش عبر `CASE WHEN current_user_can_view_cost()`
   كما في الأعلى — والدالتان `SECURITY DEFINER` مربوطتان بـ `auth.uid()`
   (هوية الجلسة الحقيقية من Supabase Auth، غير قابلة للتلاعب من العميل)،
   لا بأي قيمة يرسلها الطلب نفسه.
3. **ليش مو `security_invoker = true`** (البديل "الأنظف" في Postgres
   15+، يخلي الـ view يرث RLS تلقائياً): لو فُعِّل، الاستعلام يشتغل
   بصلاحيات المستخدم المستدعي فعلياً — و`authenticated` أصلاً بلا أي
   `SELECT` على `cost_per_night`/`target_margin_bps` في الجدول الأصلي
   (migrations 0016/0018 تمنحان أعمدة محددة فقط). النتيجة: خطأ صلاحية
   **لكل المستخدمين حتى من يملك `can_view_cost=true`** — Postgres
   يتحقق من صلاحية العمود المرجعي وقت التحليل، بغض النظر عن كون `CASE`
   سيُخفيه لاحقاً لبعض الصفوف فقط. نمط "مالك مميّز + `WHERE` يدوي" هو
   الخيار الوحيد الممكن لإخفاء عمود بهذا الشكل تحديداً.

**التغطية بالاختبارات (اتصال Postgres حقيقي، لا mock، تبديل جلسة فعلي
عبر `sign_in_as`):**
- `tests/integration/test_cost_tables_rls.py::test_sales_without_can_view_cost_sees_null_via_view`
- `tests/integration/test_cost_tables_rls.py::test_view_masks_cost_without_hiding_other_columns`
- `tests/integration/test_price_rules_rls.py::test_masking_view_hides_margin_and_min_profit_without_cost_visibility`

**القاعدة العملية:** أي view مستقبلي بنفس النمط (مالك يتجاوز RLS + `CASE`
لإخفاء عمود) سيحمل نفس الشارة دائماً — لا تُعامَل كخلل بحد ذاتها. الفحص
الحقيقي: هل شرط `WHERE` داخل الـ view يطابق سياسة `FOR SELECT` على
الجدول الأصلي حرفياً، وهل كل عمود مالي محمي بدالة `SECURITY DEFINER`
مربوطة بـ`auth.uid()` لا بقيمة من الطلب.

---

## ٩. التكامل مع أنظمة العميل

| النظام | الاتجاه | الحالة |
|---|---|---|
| AMR (المخزون) | العميل يدخل الحصة عندنا يدوياً | **الافتراضي** — لا يعتمد على API |
| AMR (لو توفر API) | قراءة الحصة آلياً | تحسين لاحق، لا يعطّل الإطلاق |
| النظام المحاسبي | دفع بيانات الحجز | يحتاج تأكيد الاسم وطريقة الربط |

**قاعدة:** لا يعتمد أي مسار حرج على نظام خارجي غير مؤكد. التكامل تحسين، لا شرط إطلاق.

---

## ١٠. البيانات الشخصية والخصوصية

هذا القسم توثيق ووعي بموضع الخطر — **لا تنفيذ**. الأعمدة/الجداول التي
لا تزال قيد التخطيط (`bookings` بحقول هوية العميل، `conversations`،
`messages`، `escalations`) لا وجود لها في `db/migrations/` اليوم؛
مذكورة هنا فقط لأنها ستحمل بيانات شخصية بمجرد بنائها، والقرار الأصوب هو
حسم الشكل قبل أول ميقريشن تنشئها، لا بعده.

### خريطة البيانات الشخصية

| الجدول.العمود | نوع البيانات | موجود اليوم؟ |
|---|---|---|
| `quotes.customer_phone` | رقم جوال العميل | **نعم** (migration 0008) |
| `quotes.conversation_id` | معرّف رقمي فقط — رابط لا بيانات شخصية بذاته | نعم، بلا FK فعلي (الجدول المُشار إليه غير موجود بعد) |
| `app_users.full_name` | اسم موظف (إداري/مبيعات) — بيانات شخصية لموظف لا عميل، فئة مختلفة | نعم (migration 0010) |
| `bookings.*` (اسم، جنسية، جوال العميل المؤكَّد) | اسم كامل، جنسية، جوال — الحقول المتوقعة بحكم طبيعة حجز فندقي حقيقي | **لا** — الجدول نفسه غير موجود بعد؛ §٤ يذكر فقط `status`/`payment_status`/`accounting_sync_status` دون حقول الهوية |
| `conversations`/`messages.*` | رقم الجوال، ونص المحادثة الكامل — قد يحوي أي تفصيل شخصي يذكره العميل طواعية | **لا** — غير موجود قبل المرحلة ٤ (نفس ما يوثقه تعليق migration 0009) |
| `escalations.*` | مرتبطة بمحادثة/عميل — تفاصيل حالة التصعيد | **لا** — غير موجود بعد |

بقية الجداول (`hotels`، `room_types`، `seasons`، `allotments`،
`room_night_inventory`، `price_rules`، `price_overrides`، `holds`،
`audit_log`) بيانات تجارية/تشغيلية بحتة — أسعار، تواريخ، كميات — لا
تحمل بيانات شخصية عن أي فرد.

**ثغرة موجودة اليوم تستحق تسجيلها هنا، لا إصلاحها الآن:** جدول `holds`
(migrations 0004-0005) **لا يحمل أي عمود يربطه بالعميل** — لا
`customer_phone` ولا ما يعادله، بخلاف `quotes` الذي يحمله من البداية.
يعني حجزاً مؤقتاً واحداً (`hold`) لا يمكن ربطه اليوم بعميل محدد إلا عبر
تتبّع خارج قاعدة البيانات. أي PR يبني تدفق الحجز الفعلي (استدعاء
`create_hold` من طبقة الوكيل، §٦-٧) يجب أن يضيف هذا العمود حينها، لا
يُترك للحجز المؤكَّد (`bookings`) وحده.

### أين تُرسَل خارج المملكة، ولأي غرض

| الوجهة | ما يُرسَل | الغرض | الحالة |
|---|---|---|---|
| **Gemini (Google)** | نص المحادثة كاملاً — كل ما يكتبه العميل، بما فيه أي بيانات شخصية يذكرها طواعية | تشغيل طبقة الوكيل (§٧) — لا بديل، هذا هو النموذج المعتمد | **قرار قائم** (§٣) — لم يُحسم إن كانت منطقة معالجة Gemini داخل المملكة أو خارجها |
| **WhatsApp Cloud API (Meta)** | رقم جوال العميل + نص كل رسالة، صادرة وواردة | القناة الوحيدة المعتمدة للتواصل مع العميل (§٣) | **قرار قائم** — البيانات تعبر بنية Meta التحتية بطبيعة استخدام القناة الرسمية، لا مفر منه طالما القناة WhatsApp |
| **Supabase (قاعدة البيانات)** | كل شيء — كل الجداول أعلاه | التخزين الأساسي | **نقطة مفتوحة موثّقة مسبقاً في §٣**: "هل يشترط العميل بقاء البيانات داخل المملكة؟" لم تُحسم. ملاحظة عملية من هذه الجلسة: المشاريع الفعلية المُنشأة حتى الآن (`hajj-umrah-system` والمشروع التجريبي `hotel-sales-agent-dev`) كلاهما في `eu-central-1` (فرانكفورت) — أي المنطقة الافتراضية المستخدمة عملياً هي خارج المملكة اليوم، لا داخلها |
| **Railway/Fly.io/Render** (خدمات FastAPI + worker) | كل طلب يمر عبر هذه الخدمات — بيانات الحجز والمحادثة أثناء المعالجة | استضافة الخدمات (§٣) | **قرار قائم، خارج المملكة** — لا يوجد من هذه المزوّدات منطقة سعودية معلنة اليوم |
| **Vercel/Railway** (لوحة الإدارة) | ما يعرضه `admin` — أسماء موظفين، وبيانات عملاء لو بُنيت شاشة تعرضها لاحقاً | استضافة الواجهة (§٣) | **قرار قائم، خارج المملكة** على الأرجح، لم يُحسم نهائياً بين الخيارين |
| **النظام المحاسبي** | بيانات الحجز، ويحتمل اسم العميل | تمرير المحاسبة (§٩) | **غير محسوم بعد** — §٩ نفسها تقول "يحتاج تأكيد الاسم وطريقة الربط"؛ لا يمكن تحديد داخل/خارج المملكة قبل تحديد النظام نفسه |

**لماذا هذا التوثيق الآن، رغم إن أغلب الوجهات "قرار قائم" أصلاً:**
نظام حماية البيانات الشخصية يفرض الإفصاح عن جهة استقبال كل نقل خارج
المملكة وغرضه — توثيقه هنا يجعل الإجابة جاهزة بدل أن تُجمَّع من عدة
قرارات متفرقة وقت الحاجة الفعلية (طلب عميل، تدقيق، أو سؤال قانوني).

### ثلاث قدرات لازم تكون ممكنة من أول تصميم، لا مُضافة لاحقاً

هذه **قرارات تصميم فقط — لا تنفيذ اليوم**. إضافتها بعد أن تتوزع بيانات
العميل بلا رابط موحّد بين عشرات الجداول مؤلمة بما يكفي لتستحق الحسم
الآن، قبل أن يُبنى أي جدول جديد يحمل بيانات عميل.

1. **حق المحو — حذف كل بيانات عميل معيّن عند الطلب.**
   يتعارض مباشرة مع `quotes` كونه **غير قابل للتعديل** (`INSERT` فقط،
   §٤) — لا يمكن حذف أو تعديل صف `quotes` بلا كسر سجل التسعير المُدقَّق
   الذي يثبت الالتزام بـ`min_allowed` وقت التسعير. المسار المتوقع، حين
   يُبنى فعلياً: **تصفير عمود `customer_phone` نفسه** (استبداله بقيمة
   تدل على "محذوف بطلب العميل") بدل حذف الصف — يبقي رقم التسعير والقاعدة
   المطبَّقة للتدقيق المالي، ويزيل الرابط بالهوية. هذا ممكن **فقط** لو
   العمود المعرِّف معزول وواضح (نفس نقطة "ثغرة `holds`" أعلاه) — لا لو
   الهاتف/الاسم مبعثر داخل حقول `jsonb` أو نصوص حرة بلا بنية.

   **المسار الفعلي — `quotes_erase_customer_phone(target_phone text)`
   (migration 0013):** دالة `SECURITY DEFINER` واحدة، لا تسوي إلا شيئاً
   واحداً — `UPDATE quotes SET customer_phone = NULL WHERE customer_phone
   = target_phone` — لا تقدر تلمس أي عمود ثاني (السعر، القاعدة المطبَّقة،
   `nights`) ولا تحذف صفاً. `GRANT EXECUTE` عليها لـ`service_role` وحده؛
   `anon`/`authenticated` مرفوضان افتراضياً أصلاً (قفل migration 0012
   العام على الدوال). النتيجة: قاعدة "لا تعديل" تبقى مفروضة على مستوى
   القاعدة نفسها لكل شيء عدا هذا الثقب الضيق المصمَّم لغرض واحد، بدل
   فتح `UPDATE` عام على `service_role` كان بيعرّض بقية الأعمدة للتعديل
   بالخطأ. مُختبَر: `tests/integration/test_customer_erasure.py`.

   **⚠️ ثغرة حقيقية لُقيت أثناء التحقق من هذا القسم، لا افتراض — أُصلحت
   بـmigration 0013:** تأكدنا فعلياً (`has_table_privilege` على مشروع
   Supabase حقيقي) إن `service_role` **كان يملك `UPDATE` و`DELETE`
   كاملين على `quotes` و`audit_log`** رغم إن ميقريشن 0008 و0011 تمنحان
   صراحةً `SELECT, INSERT` فقط — نفس آلية default ACL موثّقة أعلاه
   بالكامل، بس ما أُغلقت يوماً لـ`service_role` (migration 0012 أغلقتها
   لـ`anon`/`authenticated` فقط). يعني ضمانة "append-only" لـ`quotes`
   و`audit_log` **كانت غير مفعّلة فعلياً على مستوى القاعدة** — أي كود
   متصل بـ`service_role` يقدر يعدّل أو يحذف سجل تسعير مُدقَّق، بلا أي
   قيد. migration 0013 تُلغي صراحةً `UPDATE`/`DELETE`/`TRUNCATE`/
   `REFERENCES`/`TRIGGER` من `service_role` على الجدولين (يبقى `SELECT,
   INSERT` فقط، مطابق للتوثيق الأصلي)، ثم تفتح الثقب الضيق أعلاه فقط.
   تحقّقنا حياً على المشروع الحقيقي: إدراج صف اختباري، تصفيره بالدالة
   (رقم التسعير بقي كما هو، الهاتف صار `NULL`)، ثم محاولة `UPDATE`
   مباشرة على نفس الصف كـ`service_role` — رُفضت فعلاً
   (`permission denied for table quotes`).
2. **حق الوصول — تصدير كل بيانات عميل معيّن.**
   يحتاج معرِّفاً واحداً يمكن البحث به عبر كل جدول يحمل بيانات ذلك
   العميل — اليوم هذا `customer_phone`. أي جدول مستقبلي (`bookings`،
   `conversations`، `messages`، `escalations`) يجب أن يحمل نفس العمود
   بنفس الاسم والشكل، لا نسخة مختلفة (`phone`، `phone_number`،
   `customer_number`...) — التشتت في التسمية يعطّل التصدير الشامل بقدر
   ما يعطّله غياب العمود أصلاً.
3. **سياسة الاحتفاظ — كم نحتفظ بسجلات المحادثات قبل الحذف التلقائي.**
   **رقم المدة نفسه قرار العميل، لا افتراض تقني** — لم يُحدَّد هنا
   عمداً؛ يُحسم مع العميل قبل بناء `conversations`/`messages`، بنفس
   منطق "نقطة مفتوحة" في §٣. الآلية التقنية اللازمة لتنفيذه، متى تحدد
   الرقم، موجودة أصلاً ولا تحتاج اختراعاً: نفس نمط `worker` الحالي
   (§٦، "الإفراج" — مهمة مجدولة تفحص كل دقيقة) يُستنسخ لمهمة جديدة تفحص
   `messages.created_at`/`conversations.last_message_at` وتحذف ما تجاوز
   المدة المقرَّرة.

**المبدأ الموحّد وراء الثلاثة:** مفتاح عميل واحد واضح (`customer_phone`
اليوم) في **كل** جدول يحمل بياناته — لا بيانات شخصية مبعثرة بلا رابط.
هذا هو الفرق بين حذف/تصدير عميل واحد بضعة استعلامات `WHERE
customer_phone = :phone` واضحة، وبين مشروع بحث يدوي في كل جدول من
الصفر وقت الطلب الفعلي — وقتها يكون التصحيح مؤلماً بالضبط كما وصفه
الطلب الذي بدأ هذا القسم.
