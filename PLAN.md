# خطة التنفيذ

**المبدأ الحاكم للترتيب:** النواة الحتمية أولاً، والوكيل أخيراً.
لا يُبنى الوكيل قبل أن يكون التسعير والمخزون مختبَرين بالكامل — لأن تصحيح خطأ في التسعير بعد إطلاق الوكيل يعني مالاً ضائعاً، لا خطأ برمجياً.

---

## المرحلة ٠ — الأساس

**المخرجات**
- مستودع GitHub، فروع محمية، `main` مقفل
- **البوابات الآلية أولاً** — قبل أي كود منتج:
  - `ruff` (فحص + تنسيق)، `mypy --strict`
  - `pytest` + `pytest-cov` مع حدود تغطية مفروضة
  - `gitleaks` لفحص الأسرار
  - `eslint` + `tsc --strict` للواجهة
  - `pre-commit` hooks محلية
- CI يشغّل كل ما سبق على كل PR، والدمج ممنوع مع بناء أحمر
- هيكل المجلدات حسب `CLAUDE.md`
- مشروع Supabase (تطوير + إنتاج منفصلان)
- `.env.example` وإعداد الأسرار
- `ARCHITECTURE.md` و `CLAUDE.md` في جذر المستودع

**معيار الإنجاز:** PR فيه مخالفة متعمدة (متغير بلا نوع، أو سر مزيف، أو اختبار فاشل) **يُرفض آلياً**. اختبر البوابات قبل ما تثق فيها.

---

## المرحلة ١ — نموذج البيانات والمخزون

**المخرجات**
- الترحيلات: الفنادق، أنواع الغرف، المواسم، الحصص، `room_night_inventory`
- القيد `inventory_never_oversold`
- RLS على كل جدول
- خدمة المخزون: توفر، حجز مؤقت، تأكيد، إفراج
- worker إفراج المهل

**الاختبارات الإلزامية**
- حجز آخر غرفة من طلبين متزامنين → نجاح واحد وفشل نظيف واحد (بمعاملات حقيقية)
- المهلة تُفرج مرة واحدة فقط
- استحالة تجاوز `total`

**معيار الإنجاز:** لا توجد واجهة ولا نموذج لغة. المخزون يعمل ويقاوم التزامن.

---

## المرحلة ٢ — محرك التسعير

**المخرجات**
- جدول المواسم بالتقويمين، وقاعدة الحدود، والأولوية
- `lib/hijri.py` — وحدة التحويل الوحيدة
- توريث القواعد: عام ← موسم ← فندق ← نوع غرفة
- التسعير لكل ليلة، سعر العرض والحد الأدنى
- السلّم الزمني ومعامل الطلب
- التجاوز اليدوي
- جدول `quotes` غير القابل للتعديل

**الاختبارات الإلزامية**
- كل حد موسم على حدة
- إقامة تعبر موسمين وثلاثة
- رمضان ٢٩ يوماً و٣٠ يوماً
- الأرضية لا تنزل تحت التكلفة
- الطلب لا يمس `min_allowed`
- ترتيب التوريث صحيح في كل تركيبة

**معيار الإنجاز:** دوال التسعير نقية وقابلة للتنبؤ، ومغطاة بالكامل.

⚠️ **بوابة:** لا تبدأ المرحلة ٣ قبل مراجعة نتائج التسعير يدوياً على عينة تواريخ حقيقية والتأكد من مطابقتها للتوقع التجاري.

---

## المرحلة ٣ — الواجهة الإدارية

**المخرجات**
- إدارة الفنادق وأنواع الغرف
- إدخال الحصة اليومية (شاشة الاستخدام اليومي — تُصمَّم للسرعة)
- **تقويم المواسم** — الشاشة المركزية للتسعير:
  - عرض السنة كاملة بالتقويمين، ملوّنة بالمواسم
  - العميل ينشئ ويعدّل ويحذف المواسم بحرية كاملة
  - ترتيب الأولوية بالسحب — العميل يقرر أيهما يفوز عند التداخل
  - تحذير مرئي عند الفجوات والتداخلات — **تحذير لا يمنع**
  - معاينة: اختر تاريخاً → يظهر الموسم المطبَّق والسعر الناتج
  - الموسم الافتراضي محمي من الحذف
- إدارة التكاليف والهوامش والأرضيات
- الصلاحيات: التكلفة لمستخدم مخوّل واحد
- سجل التغييرات

**معيار الإنجاز:** موظف المبيعات يقدر يدير الحصة والأسعار كاملة بدون مطوّر.

---

⚠️ **بوابة قبل المرحلة ٤ — بوابة صلاحيات دورية إلزامية على أي PR
يلمس `db/migrations/`:** المرحلة ٤ أول مكان يضيف جداول شخصية جديدة
بوتيرة عالية (`conversations`، `messages`، وما بعدها) — بالضبط النمط
اللي كشف فجوة `service_role` على `quotes`/`audit_log` (migration
0013) وفجوة قفل الدوال بـmigration 0012. السويت المحلي وحده لا يكفي
مهما كان أخضر — `tests/supabase_default_acl_baseline.sql` محاكاة
مبنية على فهمنا اليوم، لا الحقيقة نفسها؛ `tests/
verify_default_acl_baseline.py` (فرع منفصل، بعد دمج PR #10) يتحقق من
هذا الفهم ضد مشروع Supabase حقيقي غير مُرحَّل أبداً، لكنه سكربت
مستقل اليوم — **لازم يصير فحصاً إلزامياً في CI على كل PR يلمس
`db/migrations/`** (لا أسبوعياً — الأسبوعي يمر بعد ما يكون الميقريشن
الخطأ اندمج أصلاً) قبل ما تبدأ هذي المرحلة تضيف جداول بوتيرة أعلى مما
شهدته المراحل ٠-٣. يحتاج: مشروع Supabase مرجعي دائم (غير مُرحَّل)،
سر CI (`ACL_BASELINE_DATABASE_URL`)، وخطوة CI جديدة مقيَّدة بـ`paths:
db/migrations/**`.

---

## المرحلة ٤ — قناة واتساب (قراءة فقط)

**المخرجات**
- ربط WhatsApp Cloud API، تحقق التوقيع، idempotency
- حالة المحادثة في قاعدة البيانات
- طبقة النموذج خلف واجهة واحدة، بنسخة مثبّتة
- الأدوات: `check_availability` و `get_quote` و `search_alternatives` فقط
- حارس المخرجات
- ثلاث لغات: عربي، إنجليزي، إندونيسي

**في هذه المرحلة الوكيل يجيب على التوفر والسعر — ولا يحجز إطلاقاً.**

**الاختبارات الإلزامية**
- ٥٠ محادثة حقيقية من سجل العميل
- ١٥ محاولة استدراج على الأقل: انتحال صفة، ضغط عاطفي، حقن تعليمات، تبديل لغة، طلب التكلفة
- المعيار: **صفر سعر خارج ما أنتجته خدمة التسعير**

⚠️ **بوابة:** لا تبدأ المرحلة ٥ قبل تحقيق صفر تجاوز على مجموعة الاختبار العدائية.

---

## المرحلة ٥ — الحجز والدفع

**المخرجات**
- **إضافة عمود يربط `holds` بالعميل (`customer_phone`، بنفس اسم عمود
  `quotes`) — غير موجود اليوم.** جدول `holds` (migrations 0004-0005)
  لا يحمل أي مفتاح يربطه بالعميل صاحب الحجز؛ بدونه لا نقدر نجاوب "وش
  حجوزات هذا العميل المعلّقة؟" بدون تتبّع خارج قاعدة البيانات. أي PR
  في هذه المرحلة يستدعي `create_hold` فعلياً من طبقة الوكيل لازم يضيف
  هذا العمود ضمن نفس الميقريشن، لا بعده — راجع ARCHITECTURE.md §١٠.
- `create_hold` و `confirm_booking`
- مهل متدرجة حسب قرب الوصول
- تذكير قبل انتهاء المهلة
- تأكيد الدفع (تحويل بنكي)
- سند تأكيد الحجز
- منطق التفاوض: عدّاد التنازلات في قاعدة البيانات، خطوات متناقصة، مقابل لكل تنازل
- قواعد التصعيد كاملة + طابور بشري
- **الكاش عند الوصول يفتح تصعيداً، لا يؤكده الوكيل**

**معيار الإنجاز:** مسار كامل من أول رسالة إلى حجز مؤكد، بدون تدخل بشري في الحالة الاعتيادية.

---

## المرحلة ٦ — المالية والتنبيهات

**المخرجات**
- لوحة: التعرض، المتحقق، المتوقع ضياعه، الفجوة عن الهدف
- الهدف الشهري بالتقويمين — **عرض فقط، لا يدخل التسعير**
- تنبيه يوم ١٥ للّيالي منخفضة التصريف
- تنبيهات: فشل مزامنة محاسبية، تصعيد بلا رد
- تمرير بيانات الحجز للنظام المحاسبي

**معيار الإنجاز:** العميل يرى وضعه المالي دون فتح أي شاشة أخرى.

---

## المرحلة ٧ — التشغيل التجريبي

- **فندق واحد، نحو ٢٠ غرفة يومياً**
- بدون أي ربط بأنظمة العميل — الحصة تُدخل يدوياً
- مراقبة يومية: كل محادثة تُراجع في الأسبوع الأول
- كل خطأ يتحول إلى حالة اختبار قبل إصلاحه

**معايير التوسع:**
- صفر بيع مزدوج
- صفر سعر تحت الأرضية
- نسبة تصعيد أقل من ٢٠٪
- رضا موظف المبيعات عن الشاشات

**لا يتم التوسع إلا بعد تحقق هذه المعايير على بيانات حقيقية.**

---

## المسارات المؤجلة (لا تعطّل الإطلاق)

| البند | متى |
|---|---|
| ربط AMR بـ API | إن تأكد وجوده — تحسين لاحق |
| ربط محاسبي آلي | بعد تأكيد اسم النظام وإمكانياته |
| بوابة دفع إلكتروني | بعد استقرار التحويل البنكي |
| إصدار فواتير من نظامنا | **خارج النطاق** — قرار معتمد |

---

## ملاحظات للتنفيذ مع Claude Code

- ابدأ كل جلسة بقراءة `CLAUDE.md` و `ARCHITECTURE.md`
- مهمة واحدة لكل جلسة، وفرع واحد لكل مهمة
- اطلب منه صراحة: الاختبارات قبل الكود في `pricing/` و `inventory/`
- راجع كل PR بنفسك — خصوصاً أي تغيير يلمس المال أو المخزون
- عند أي انحراف عن المعمارية، أوقفه وأعده للوثيقة بدل التفاوض معه داخل الجلسة
